# truefix-core

The foundational TrueFix types: FIX messages and fields, plus SOH framing with correct
`BodyLength` and `CheckSum` handling.

Most applications should depend on the [`truefix`](https://crates.io/crates/truefix) facade crate.

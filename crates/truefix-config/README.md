# truefix-config

Configuration parsing and models for TrueFix `SessionSettings` and FIX `.cfg` files.

See the [TrueFix repository](https://github.com/zhangjiayin/truefix) for configuration examples.

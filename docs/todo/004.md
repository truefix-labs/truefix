# TrueFix Bug Review — Items NOT Tracked in `docs/todo/003.md`

> **Historical audit snapshot.** Later remediation may have resolved or narrowed these findings.
> Preserve the per-item evidence, but use current code/tests and the
> [documentation index](../README.md) for present status.

> Generated 2026-07-03 from two review passes:
> 1. **Pure code review** — 7 parallel deep-dive reviews (one per crate), each reading all source files.
> 2. **QFJ comparison** — 5 parallel deep-dive comparisons against `thrdpty/quickfixj` (Session,
>    DataDictionary, codec, store, transport), plus manual QFJ `Session.java` analysis.
>
> Every item below was verified directly against current TrueFix source. Items already tracked
> in `003.md` (BUG-05–BUG-22, GAP-48–GAP-56, B1–B30) are **excluded** — only genuinely new
> findings appear here. A small number of "partially covered" items (where 003.md touches the
> same area but misses the specific defect) are listed at the end with the 003.md reference.
>
> Numbering continues from 003.md: `BUG-23` onwards.

---

## P0 — Critical (protocol-correctness break, data-loss risk, or DoS vector)

### BUG-23: `frame_length` integer overflow — remotely exploitable

**TF**: `crates/truefix-core/src/framing.rs:37`
```rust
let total = body_start + body_len + 7;
```
`body_len` is parsed from the wire as `usize` with no upper-bound check. A malicious sender
crafts `9=18446744073709551615`; the addition overflows. In release mode `total` wraps to a small
value, `buf.len() >= total` is true, and `frame_length` returns `Ok(Some(total))` — the caller
drains the wrong number of bytes and the stream desynchronizes. Exploitable by any unauthenticated
peer. Fix: use `checked_add`.

### BUG-24: Checksum `u32` overflow — debug panic

**TF**: `crates/truefix-core/src/codec/decode.rs:187` and `encode.rs:55`
```rust
let computed: u32 = pre.iter().map(|&b| u32::from(b)).sum::<u32>() & 0xFF;
```
`Iterator::sum::<u32>()` panics on overflow in debug builds when the message exceeds ~16.8 MB.
Violates the crate's "No path panics" invariant. Fix: use `wrapping_add` or a `u64` accumulator.

### BUG-25: Scheduled initiator never reconnects after transient connection drop

**TF**: `crates/truefix-transport/src/lib.rs`, `run_scheduled_initiator` (~L960-995).
`connect_initiator_with` → `spawn_session_io` spawns `run_connection` in a separate tokio task
and returns the `SessionHandle` immediately. The `JoinHandle` is never awaited, so when the
connection drops `current` remains `Some(handle)` forever. The reconnect condition
`current.is_none()` never becomes true. Directly contradicts the code's own comment ("keep
retrying a connection while in session and disconnected, e.g. after a transient network drop").
Distinct from 003.md BUG-09 (spurious store reset on restart — same function, different bug).

### BUG-26: SQL Store migration-order bug — breaks upgrading pre-existing databases

**TF**: `crates/truefix-store/src/sql.rs`, `SqlStore::ensure_schema` (~L230-285).
`INSERT ... creation_time ...` for the sessions table runs **before**
`add_creation_time_column_if_missing()`. For a sessions table created before the `creation_time`
column existed, `CREATE TABLE IF NOT EXISTS` is a no-op and the INSERT references a non-existent
column. `ON CONFLICT`/`IGNORE` suppresses constraint violations, not schema errors. Affects all
three SQL backends (SQLite/Postgres/MySQL). Fix: call `add_creation_time_column_if_missing()`
before the INSERT.

### BUG-27: `Engine::shutdown()` cannot stop regular initiators + no `Drop` impl

**TF**: `crates/truefix/src/lib.rs`, `Engine::shutdown` (~L420-426) and `Engine` struct.
`shutdown()` aborts `self.acceptors` and stops `self.failover_initiators`, but never touches
`self.initiators` (`Vec<SessionHandle>`). `SessionHandle` exposes only `logout()` (async) and
`join(self)` — its inner `JoinHandle` is private with no `abort`. So even an explicit
`shutdown()` cannot terminate these tasks. Additionally, `Engine` has no `Drop` impl — if a
caller drops `Engine` without calling `shutdown()`, every spawned tokio task is detached
(acceptors keep accepting, initiators keep running, failover loops keep reconnecting forever).
Distinct from 003.md BUG-21 (which notes the doc-comment asymmetry; this is the stronger finding
that there is no available API to clean up at all).

### BUG-28: ResetSeqNumFlag handshake completely broken

**TF**: `crates/truefix-session/src/admin.rs` `logon()` + `crates/truefix-session/src/state.rs` `on_logon()`.

Two compounding defects:
1. **Acceptor does not echo `ResetSeqNumFlag=Y`**: `admin::logon()` sets the flag based on
   `config.reset_on_logon` (a static config), not on whether the incoming Logon had it. QFJ's
   `generateLogon(otherLogon, ...)` sets it based on `state.isResetReceived()`.
2. **Initiator does not verify the echo, nor infer from seq=1 (QFJ-383)**: `on_logon()` has no
   `reset_sent`/`reset_received` tracking. QFJ disconnects with "Expected Logon response to have
   reset sequence numbers" if the acceptor didn't acknowledge, and infers the flag from seq=1
   when absent.

Combined: the entire ResetSeqNumFlag mechanism is non-functional. Related to 003.md B1 (which
covers the `full = !self.logon_sent` partial-reset issue), but the echo/verification/inference
defects are distinct.

### BUG-29: ResendRequest with too-high sequence number deferred — potential deadlock (QFJ-673)

**TF**: `crates/truefix-session/src/state.rs`, `on_received()` `Ordering::Greater` branch.
A too-high ResendRequest is queued and `build_resend()` is NOT called. The ResendRequest is only
processed later from `drain_queue()` when the gap is filled. QFJ's `nextResendRequest()` calls
`verify(rr, false, validateSequenceNumbers)` — deliberately skipping the too-high check (QFJ-673)
and processing the resend immediately. Deadlock scenario: we expect seq 3; counterparty sends
ResendRequest(seq=5); we queue it and send our own ResendRequest for 3–4; counterparty waits for
our response to their ResendRequest(5); we wait for them to fill our gap(3–4) — deadlock.

### BUG-30: `SeqFile::write` uses truncate-then-write — crash loses both sequence numbers

**TF**: `crates/truefix-store/src/file.rs`, `SeqFile::write`.
Opens the seqnums file with `.truncate(true)` — zeros the file before writing. This runs on
every `set_next_sender_seq`/`set_next_target_seq` call (i.e., every message). A crash between
truncate and write leaves the file empty. QFJ uses `seek(0)` + `writeUTF()` without truncation,
and keeps sender/target in separate files. Distinct from 003.md B17 (which is about
`FileStore::reset()` non-atomicity; this affects the regular per-message write path).

### BUG-31: Corrupted seqnums file silently defaults to (1, 1)

**TF**: `crates/truefix-store/src/file.rs`, `load_seqnums`.
`unwrap_or(1)` on parse failure silently defaults to 1. Combined with BUG-30, a crash guarantees
silent corruption: the session restarts from (1, 1) with no error logged. QFJ's
`initializeSequenceNumbers()` throws `IOException` on corrupted data, preventing session startup.

### BUG-32: No duplicate/competing connection protection on acceptor

**TF**: `crates/truefix-transport/src/lib.rs`, `route_and_run` / `Acceptor::serve`.
QFJ's `AcceptorIoHandler` checks `qfSession.getResponder() != null` when a Logon arrives — if
the session is already bound to an active connection, it closes the new one. TrueFix has no such
check. A second connection presenting the same SessionID creates a competing session, leading to
sequence-number corruption and duplicate message processing.

### BUG-33: `reset_on_disconnect` not honored when TCP connection drops

**TF**: `crates/truefix-transport/src/lib.rs`, `run_connection`.
When the TCP connection drops, `run_connection` exits and calls `write_half.shutdown()` +
`on_logout`, but **never dispatches any event to the `Session` state machine**. The `Session` is
simply dropped. `enter_disconnected()` (which checks `reset_on_disconnect || reset_on_logout`)
is only called from within the state machine on Logout-received/logon-timeout/heartbeat-timeout.
The `Event` enum has no `Disconnected` variant. For the reconnecting initiator, the store
retains stale sequence numbers; `ResetOnDisconnect` is silently ignored.

### BUG-34: `from_app`/`from_admin` called before session-level validation

**TF**: `crates/truefix-transport/src/lib.rs`, `process_inbound` (~L970-990).
TrueFix calls `from_admin`/`from_app` **before** dispatching to the session state machine. QFJ
calls `fromCallback()` inside `verify()` — after all session-level checks (sequence, identity,
latency, PossDup) pass. Consequence: application callbacks receive messages that the session
layer hasn't validated (gap fill not yet processed, dictionary validation not yet run, identity
not yet confirmed). If the message is later rejected or the session is torn down, the application
has already processed it.

---

## P1 — High/Medium (real defect, narrower blast radius)

### BUG-35: `resend_requested` not reset on reconnect — suppresses new ResendRequest

**TF**: `crates/truefix-session/src/state.rs`, `on_connected()`.
`on_connected` does not reset `resend_requested` or `queue`. 003.md B4 covers
`resend_target`/`resend_chunk_end`/timers, but the `resend_requested` flag and `queue` are also
stale. On reconnect, `on_logon`'s `Ordering::Greater` arm is guarded by
`if !self.resend_requested` — a new gap will not trigger a ResendRequest. The session silently
loses the gap.

### BUG-36: `on_tick` heartbeat-timeout arithmetic can overflow `u32`

**TF**: `crates/truefix-session/src/state.rs`, `on_tick()` `LoggedOn` arm.
`hb * self.config.heartbeat_timeout_multiplier.max(1) + 2` is plain `u32` arithmetic. Large values
overflow → panic in debug, silent wrap in release. Additionally,
`heartbeat_timeout_multiplier` is parsed as `u32` (losing fractional precision — QFJ uses
`double`, default 1.4). A `.cfg` with `HeartBeatTimeoutMultiplier=1.4` truncates to 1, yielding a
2.25× shorter timeout than QFJ. Fix: use `saturating_mul`/`saturating_add` and change the field
type to `f64`.

### BUG-37: `decode_with_groups` never sets `fields_out_of_order`

**TF**: `crates/truefix-core/src/codec/decode.rs:76-96`.
`decode()` performs section-ordering validation (sets `fields_out_of_order = true`); the
group-aware `decode_with_groups` does none of this. A grossly out-of-order message decoded via
this path reports `fields_out_of_order() == false` unconditionally. 003.md GAP-26 notes the
function is dormant, but doesn't track that even if called, the ordering check is missing.

### BUG-38: Data field tag not verified against the expected data tag

**TF**: `crates/truefix-core/src/codec/decode.rs:251`.
`data_field_for_length(tag)` returns the expected data tag (e.g. 95→96), but only `.is_some()`
is checked — the return value is discarded. If a non-data field follows a length field, the
parser silently consumes `len` bytes from the wrong field, corrupting all subsequent parsing.
003.md B22 is about missing length/data pairs in the whitelist; this is about the verification
logic itself being broken for existing pairs.

### BUG-39: Plain reconnecting initiator skips socket options

**TF**: `crates/truefix-transport/src/lib.rs`, `connect_initiator_reconnecting_multi` (~L885-915).
On a successful connect it calls `run_connection(stream, ...)` without calling
`services.socket_options.apply(&stream)`. The TLS variant correctly applies them. The plain
reconnecting initiator never sets `tcp_no_delay`, `keep_alive`, buffer sizes, etc.

### BUG-40: `logon_sent` not reset on AwaitingLogon timeout

**TF**: `crates/truefix-session/src/state.rs`, `on_tick()` → `enter_disconnected`.
When logon timeout fires and `reset_on_disconnect`/`reset_on_logout` are not configured,
`enter_disconnected` does not reset `logon_sent`. On reconnect, `seed_sequences` is a no-op
(guarded by `if !self.logon_sent`), so the transport cannot re-seed sequence numbers from the
durable store.

### BUG-41: MSSQL `save_and_advance_sender` does not rollback on COMMIT failure

**TF**: `crates/truefix-store/src/mssql.rs`, `save_and_advance_sender` (~L290-330).
If `COMMIT TRANSACTION` returns an error, the code returns `Err(e)` but never issues `ROLLBACK`,
leaving an open transaction on the mutex-held connection. The error branch (statement failure)
correctly rolls back; only the COMMIT-failure case is mishandled.

### BUG-42: No `validLogonState` check — messages processed before logon completes

**TF**: `crates/truefix-session/src/state.rs`, `on_received()`.
`on_received()` has no state check. A non-Logon message arriving in `AwaitingLogon` state is
processed normally — if its seq is too high, TrueFix queues it and sends a ResendRequest
(protocol violation: no ResendRequests before logon). QFJ's `validLogonState()` rejects non-
Logon/non-Logout/non-SequenceReset/non-Reject messages when not logged on, causing a disconnect.

### BUG-43: `NextExpectedMsgSeqNum(789)` too-high not rejected

**TF**: `crates/truefix-session/src/state.rs`, `on_logon()` (~L900).
TrueFix only handles the too-low case (resends the gap). If 789 > `next_out_seq` (counterparty
expects messages we never sent), TrueFix silently ignores it, leaving the session desynced. QFJ
disconnects with "Tag 789 is higher than expected."

### BUG-44: Logon without `MsgSeqNum` (tag 34) is silently accepted

**TF**: `crates/truefix-session/src/state.rs`, `on_logon()` (~L835).
`logon_seq` is `None` when tag 34 is absent; `disposition` is `None`; the `Some(Ordering::Equal)`
guard doesn't fire, so `next_in_seq` is not advanced. But the state still transitions to
`LoggedOn` and a Logon response is sent. The session is now desynced. QFJ checks `MsgSeqNum`
presence at the top of `next()` for all messages.

### BUG-45: `load_index` reads entire body file into memory — OOM on open

**TF**: `crates/truefix-store/src/file.rs`, `load_index` (called by `BodyLog::open`).
`fs::read(&path)` reads the entire body file (all message bytes ever stored) into a `Vec<u8>`
to build the offset index. For a long-running session with 1M messages at ~500 bytes each, this
allocates ~500 MB just to open. QFJ's `FileStore` reads only a separate compact header/index
file (16 bytes per message) and never touches the body file at open time.

### BUG-46: `frame_length` does not verify CheckSum field at the computed position

**TF**: `crates/truefix-core/src/framing.rs`, `frame_length()`.
Computes `total = body_start + body_len + 7` and returns `Ok(Some(total))` whenever
`buf.len() >= total` — without checking that bytes at `body_start + body_len` start with `10=`.
A wrong BodyLength makes `total` point at the wrong position; the transport drains the wrong
number of bytes and desynchronizes. QFJ's `FIXMessageDecoder` verifies `10=???<SOH>` at the
expected position and rescans on mismatch.

### BUG-47: `as_int()`/`as_decimal()` trim whitespace QFJ rejects

**TF**: `crates/truefix-core/src/field.rs`, `as_int`/`as_decimal`.
Uses `s.trim().parse()`, accepting leading/trailing whitespace (including Unicode whitespace)
and `+`-prefixed integers. QFJ's `IntConverter`/`DecimalConverter` do not trim and reject these.

### BUG-48: Timestamp parsing accepts non-standard fractional digit counts

**TF**: `crates/truefix-core/src/field.rs`, `parse_utc_timestamp`/`parse_utc_time_only`.
Accepts any number of fractional digits ≥ 1. QFJ accepts only 0/3/6/9/12 digits (timestamp) or
0/3/6/9/12 (time-only). `20240101-12:00:00.1` (1 digit) and 13+ digits are accepted by TrueFix
but rejected by QFJ.

### BUG-49: `tokenize` silently ignores invalid data-length field values

**TF**: `crates/truefix-core/src/codec/decode.rs`, `tokenize`.
When a length field (e.g. tag 95) has a non-numeric value, `parse_usize(&value)` returns `None`,
`pending_data_len` is never set, and the following data field is parsed as normal text with no
error. QFJ throws `FieldConvertError` → `InvalidMessage`.

### BUG-50: `ReconnectHandle::stop()` cannot stop a currently-connected initiator

**TF**: `crates/truefix-transport/src/lib.rs`, `ReconnectHandle::stop`.
`stop()` only sets an `AtomicBool`. The reconnect loop checks it at the top of the `while` loop
and after `run_connection` returns. But if a connection is currently active, the loop is blocked
inside `run_connection`. `ReconnectHandle` doesn't hold the `control_tx` sender. There is no
`logout()` method either. The initiator keeps running indefinitely until the connection drops.

### BUG-51: Multi-endpoint rotation not reset after successful connect

**TF**: `crates/truefix-transport/src/lib.rs`, `connect_initiator_reconnecting_multi`.
`next_endpoint` is incremented before each attempt and never reset to 0 after a successful
connect. After a connection to endpoint N drops, the next reconnect tries N+1 (wrapping) instead
of preferring the primary. QFJ resets `nextSocketAddressIndex = 0` on successful connect.

### BUG-52: Acceptor does not validate that the first message is a Logon

**TF**: `crates/truefix-transport/src/lib.rs`, `route_and_run`.
`route_and_run` reads the first complete message, decodes it, and uses its comp-ID fields for
routing — but never checks `MsgType == A`. A non-Logon first message is routed, potentially
creating a session from a dynamic template with incorrect comp IDs. QFJ refuses immediately.

### BUG-53: Unbounded admin channel — no backpressure safety valve

**TF**: `crates/truefix-transport/src/lib.rs`, `read_loop`.
Uses `mpsc::unbounded_channel()` for all administrative traffic. If the processing loop is
stalled (e.g. slow `from_app`), the admin channel grows without bound — OOM. QFJ uses bounded
queues (capacity 10000) with backpressure.

### BUG-54: Group required fields not recursively validated

**TF**: `crates/truefix-dict/src/validate.rs`, `validate()`.
QFJ's `checkHasRequired` recursively enters each group entry and checks the group's own
`requiredFields`. TrueFix only checks the message body's `mdef.required` — it never recursively
validates required fields within group entries. A group entry missing a required delimiter is
not caught (if the count declares correctly).

### BUG-55: Group field types not validated for group-decoded messages

**TF**: `crates/truefix-dict/src/validate.rs`, `validate_groups()`.
QFJ's `iterate()` recurses into `map.getGroups()` and validates each group entry's fields via
the group's own `DataDictionary`. TrueFix's `validate_groups` only works for flat-decoded
messages (where `body.fields()` yields group members). If decoded via `decode_with_groups`,
group members are in `Member::Group` and `body.fields()` skips them — these fields are never
type/enum-checked.

### BUG-56: Outbound messages do not include SubID/LocationID

**TF**: `crates/truefix-session/src/admin.rs` `base()` + `crates/truefix-session/src/state.rs` `send_app()`.
GAP-47 (feature 005) added `sender_sub_id`/`sender_location_id`/`target_sub_id`/
`target_location_id` to `SessionConfig`, but `admin::base()` and `send_app()` never use them.
All outbound messages are missing SubID(50/142) and LocationID(57/143). QFJ's `initializeHeader`
sets all six identity fields. Configured sessions emit messages the counterparty cannot route.

### BUG-57: PossDup anti-replay check only executes on `Ordering::Less`

**TF**: `crates/truefix-session/src/state.rs`, `on_received()`.
`poss_dup_anti_replay_violation` is only called in the `Ordering::Less` branch. QFJ's
`validatePossDup` runs for all `PossDupFlag=Y` messages (including expected-seq and too-high).
A `PossDupFlag=Y` message with `OrigSendingTime > SendingTime` and seq == expected is not
rejected by TrueFix.

### BUG-58: Reject(35=3) missing `RefMsgType(372)`

**TF**: `crates/truefix-session/src/admin.rs`, `reject_with_reason()` and `reject()`.
FIX 4.2+ requires Reject to include `RefMsgType(372)`. QFJ sets it when
`beginString >= FIX.4.2`. TrueFix never sets it on any Reject.

### BUG-59: `SessionRejectReason(373)` not version-filtered

**TF**: `crates/truefix-session/src/admin.rs`, `reject_with_reason()`.
QFJ filters by version: FIX ≤ 4.1 doesn't set the field; FIX 4.2 only codes ≤ 11; FIX 4.3 only
≤ 15; FIX 4.4 only ≤ 16 or `OTHER`. TrueFix unconditionally sets it for all versions — a
protocol violation for FIX 4.0/4.1.

### BUG-60: Logout response does not echo received `Text(58)`

**TF**: `crates/truefix-session/src/state.rs`, `on_logout_msg()`.
When receiving a Logout, TrueFix's response has `text = None`. QFJ's `generateLogout(otherLogout)`
copies `LastMsgSeqNumProcessed` from the received Logout and the transport echoes text.

### BUG-61: Acceptor-group `Services`/store built only from the first member

**TF**: `crates/truefix/src/lib.rs`, acceptor-group block (~L255-330).
The shared `Services` (store, log, socket_options, validator) is built exclusively from
`primary = members.first()`. If two sessions sharing a port configure different
`FileStorePath`/`JdbcURL`, the second session's messages/sequence numbers are written to the
first session's store — silent state corruption. 003.md BUG-16 covers
`ContinueInitializationOnError` taking the first member's flag; this is the store/log/validator
silently using the wrong session's storage.

### BUG-62: `CHAR` field validation not version-sensitive

**TF**: `crates/truefix-dict/src/model.rs`, `FieldType::value_ok`, `Self::Char` arm.
QFJ skips `CharConverter` validation for FIX ≤ 4.1 (treats CHAR as String). TrueFix
unconditionally calls `field.as_char()` for all versions — FIX 4.0/4.1 CHAR fields with
multi-character values are rejected by TrueFix but accepted by QFJ.

---

## P2 — Low (minor, low-probability, or tooling/hygiene)

### BUG-63: `precision_key` is case-sensitive

**TF**: `crates/truefix-config/src/builder.rs`, `precision_key` (~L355-372).
Requires exact uppercase (`SECONDS`/`MILLIS`/`MICROS`/`NANOS`). Every other parser in the same
file is case-insensitive. `TimeStampPrecision=seconds` fails with `InvalidValue`.

### BUG-64: `bool_key` silently treats any non-"Y" value as `false`

**TF**: `crates/truefix-config/src/builder.rs`, `bool_key` (~L245-249).
`SocketUseSSL=true` silently disables TLS. No error for invalid values. May mirror QFJ's strict
Y/N grammar, but the silent acceptance of arbitrary strings as `false` is a robustness concern.

### BUG-65: `parse_utc_offset` integer overflow

**TF**: `crates/truefix-config/src/builder.rs`, `parse_utc_offset` (~L810-820).
`hours * 3600` can exceed `i32::MAX` for large values — debug panic.

### BUG-66: `prepare_resend` ignores `timestamp_precision`

**TF**: `crates/truefix-session/src/admin.rs`, `prepare_resend`.
The refreshed `SendingTime` calls `now_utc_timestamp()` (hardcoded `Milliseconds`), not
`now_utc_timestamp_prec(self.config.timestamp_precision)`. Resent messages always use
millisecond precision regardless of session configuration.

### BUG-67: Acceptor truncates peer's `HeartBtInt` via `as u32`

**TF**: `crates/truefix-session/src/state.rs`, `on_logon()`, `Role::Acceptor` arm.
`self.config.heartbeat_interval = hbi as u32` where `hbi` is `i64`. A peer-supplied value >
`u32::MAX` silently truncates to garbage. Should clamp.

### BUG-68: `MsgSeqNum too low` path skips `reset_on_error()`

**TF**: `crates/truefix-session/src/state.rs`, `on_received()`, `Ordering::Less` non-PossDup branch.
Latency-failure and identity-failure paths both call `reset_on_error()` after disconnecting. The
hard sequence-violation path does not — inconsistent if `ResetOnError` is configured.

### BUG-69: `on_tick` `AwaitingLogout` does not send heartbeats

**TF**: `crates/truefix-session/src/state.rs`, `on_tick()`.
`AwaitingLogout` (not yet timed out) falls through to `_ => {}` — no heartbeats. If
`logout_timeout` is long, the peer may disconnect us for heartbeat failure before our timeout.

### BUG-70: MSSQL URL parser breaks on passwords containing `@`

**TF**: `crates/truefix-store/src/mssql.rs` + `crates/truefix-log/src/mssql.rs`, `parse_url`.
`split_once('@')` splits at the first `@`. A password containing `@` is mis-split. 003.md GAP-55
is about `splice_credentials` not percent-encoding — this is a different function (`parse_url`)
with a different root cause.

### BUG-71: `tcp_connect` blocking-connect thread/socket leak on timeout

**TF**: `crates/truefix-transport/src/lib.rs`, `tcp_connect` (~L175-200).
When `local_bind_addr` is set, `spawn_blocking` runs a blocking `connect()`. On timeout, the
future is dropped but the `spawn_blocking` thread keeps running until the OS TCP timeout.

### BUG-72: codegen `type_mapping` omits `TIME`/`PRICEOFFSET` and other US9 types

**TF**: `crates/truefix-dict/src/codegen.rs`, `type_mapping`.
Only maps `INT`/`FLOAT`/`CHAR`/`BOOLEAN`/`UTCTIMESTAMP` explicitly; everything else falls to
`&str`. `TIME` (same wire format as `UtcTimestamp`) and `PRICEOFFSET` (decimal) get string
accessors instead of typed ones.

### BUG-73: codegen enum-variant labels that are Rust keywords are not sanitized

**TF**: `crates/truefix-dict/src/codegen.rs`, `sanitize_variant`.
Only prefixes leading digits. A label equal to a Rust keyword (`Type`, `Self`, `Match`, etc.)
produces uncompilable generated code.

### BUG-74: codegen vs runtime track divergence on `open` enum modifier

**TF**: `crates/truefix-dict/src/codegen.rs` vs `crates/truefix-dict/src/parser.rs`.
Runtime parser recognizes and strips a bare `open` token after the field type. Codegen's
`parse_dict` does not — it would ingest `open` as an enum value. Dual-track divergence.

### BUG-75: `on_logon` in `Disconnected` state still processes sequence logic

**TF**: `crates/truefix-session/src/state.rs`, `on_logon()`.
If a Logon arrives when `state == Disconnected` (transport race), the role/state guards fail
(`_ => {}`), so no logon response is sent. But the `disposition` / `NextExpectedMsgSeqNum`
logic still runs, potentially advancing `next_in_seq` or issuing a spurious ResendRequest on a
session that isn't actually connected.

### BUG-76: Acceptor-group TLS can come from a different member than the store/Services

**TF**: `crates/truefix/src/lib.rs` (~L300).
`Services` is sourced from `members.first()`; TLS is sourced from `members.iter().find_map(...)`
— the first member that has TLS. When the primary has no TLS but a later member does, the listener
ends up with TLS material from one session and the store/log/validator from a different one.

### BUG-77: `Monitor` never wired by `Engine::start`

**TF**: `crates/truefix/src/lib.rs` (~L285,345).
`Services` has a `monitor: Option<Monitor>` field that `Engine::start` always leaves as `None`.
The transport's operational API (`force_logout`/`reset`/`send_app`/`status`/`is_connected`) is
unreachable for any session started through the engine facade.

### BUG-78: Timestamp parsing rejects leap seconds (sec=60) that QFJ accepts

**TF**: `crates/truefix-core/src/field.rs`, `parse_utc_timestamp`.
`time::Time::from_hms_nano` rejects sec=60. QFJ explicitly maps `s == 60` → `s=59, ns=999999999`.

### BUG-79: `frame_length` does not validate BeginString format

**TF**: `crates/truefix-core/src/framing.rs`, `frame_length()`.
Only checks `buf.get(0..2) == Some(b"8=")`. QFJ requires `8=FIX.X.X` or `8=FIXT.X.X` pattern.
Non-FIX data is framed by TrueFix.

### BUG-80: `decode` accepts messages with no `MsgType` / `BodyLength=0`

**TF**: `crates/truefix-core/src/codec/decode.rs`, `tokenize_validated`.
Only requires field 1=BeginString, field 2=BodyLength. Does not require field 3=MsgType.
`8=FIX.4.4\x019=0\x0110=086\x01` (no MsgType) is accepted.

### BUG-81: `CachedFileStore::open_with_options` materializes all message bodies on open

**TF**: `crates/truefix-store/src/file.rs`, `CachedFileStore::open_with_options`.
`body.all()` reads every stored message body into a `Vec` before cache insertion. Peak memory
is ALL message bodies, not `max_cached_msgs`. QFJ's `CachedFileStore` never reads bodies on open.

### BUG-82: Stale session status in `Monitor` after connection drop

**TF**: `crates/truefix-transport/src/lib.rs`, `run_connection` shutdown path.
`mark_disconnected` sets `connected: false` but does not update `status`. `monitor.status()`
returns `LoggedOn` while `is_connected()` returns `false` — inconsistent snapshot. Consequence
of BUG-33 (session state machine never notified of disconnection).

### BUG-83: GapFill `SequenceReset` missing `OrigSendingTime(122)`

**TF**: `crates/truefix-session/src/admin.rs`, `sequence_reset()`.
QFJ's `generateSequenceReset` sets `OrigSendingTime = SendingTime`. TrueFix does not. Strict
peers may require `OrigSendingTime` on all `PossDupFlag=Y` messages.

### BUG-84: FIX < 4.2 not forced to seconds-precision timestamps

**TF**: `crates/truefix-session/src/admin.rs` `base()` + `state.rs` `send_app()`.
QFJ forces `UtcTimestampPrecision.SECONDS` for `beginString < FIX.4.2`. TrueFix always uses
`config.timestamp_precision` (default `Milliseconds`) — FIX 4.0/4.1 sessions emit
`YYYYMMDD-HH:MM:SS.sss`, a protocol violation for those versions.

### BUG-85: Acceptor also gets Logon timeout

**TF**: `crates/truefix-session/src/state.rs`, `on_tick()`.
`AwaitingLogon` timeout applies to both roles. QFJ's `next()` only times out when
`state.isLogonAlreadySent()` is true (initiator only). Acceptors wait indefinitely for a Logon.

---

## Partially covered (003.md touches the same area but misses this specific defect)

| Item | 003.md ref | What's different |
|------|-----------|-------------------|
| BUG-23 (frame_length overflow) | B30 mentions "same overflow bug" as a footnote about code duplication; BUG-13 tracks the DoS angle. Neither tracks the specific `checked_add` fix as its own P0 item. |
| BUG-37 (decode_with_groups missing fields_out_of_order) | GAP-26 notes the function is dormant. Doesn't track that even if called, the ordering check is absent. |
| BUG-54/BUG-55 (group validation gaps) | GAP-26 notes header/trailer group members are mishandled. Doesn't track that body group required-field and type validation are also broken for group-decoded messages. |
| ~~`Exchange` format check accepts lowercase~~ | ⚠️ **二次核实：整条撤回。** Re-checked QFJ's `checkValidFormat` (`DataDictionary.java`) and QFGo's `validation.go` — neither reference enforces case on EXCHANGE fields at all; both treat it as an unvalidated plain string. There is no "documented uppercase requirement" in either reference to be more lenient than, so this row mischaracterizes both QFJ and QFGo. Not a real finding. |

---

## Summary

| Severity | Count |
|----------|-------|
| P0 (Critical) | 15 (BUG-23–BUG-34, BUG-86–BUG-88) |
| P1 (High/Medium) | 43 (BUG-35–BUG-62, BUG-89–BUG-103) |
| P2 (Low) | 31 (BUG-63–BUG-85, BUG-104–BUG-111) |
| Partially covered | 4 |
| **Total** | **93** |

### Suggested triage order

1. **BUG-23** (frame_length overflow) — remotely exploitable, one-line fix
2. **BUG-28 + BUG-29** (ResetSeqNumFlag handshake + ResendRequest deadlock) — protocol correctness
3. **BUG-32 + BUG-33** (duplicate connections + reset_on_disconnect) — transport correctness
4. **BUG-30 + BUG-31** (SeqFile crash corruption) — data durability
5. **BUG-34** (callback ordering) — application-facing correctness
6. **BUG-25 + BUG-27** (initiator reconnect + Engine cleanup) — operational
7. **BUG-26** (SQL migration) — deployment blocker
8. **BUG-24** (checksum panic) — one-line fix
9. **BUG-86 + BUG-87** (schedule awareness) — acceptor silently ignores schedule, accepts Logons outside trading hours (QFJ+QFGo both enforce)
10. **BUG-98** (reset before verify) — malformed Logon with ResetSeqNumFlag causes data loss before rejection
11. **BUG-88** (forceResendWhenCorruptedStore semantics) — flag doesn't do what QFJ users expect
12. **BUG-89** (admin message validation) — malformed admin messages pass unvalidated
13. **BUG-100 + BUG-101** (parser robustness) — zero BodyLength accepted, garbage kills connection
14. **BUG-92 + BUG-109** (ResetSeqNumFlag on fresh start) — interop with QFJ/QFGo
15. **BUG-93** (onLogout missed during logon handshake) — application callback gap
16. **BUG-94** (logon retry backoff) — 200ms tight loop hammers peer
17. Everything else in P1/P2 — schedule alongside normal work

> ⚠️ **二次核实（见下方"二次交叉验证"章节）**：#1 (BUG-23) 已被 `MAX_BODY_LEN` 上限间接修复，
> 溢出路径不再可达，可降级为纵深防御式的低优先级加固；#10 (BUG-98) 的 reset/verify 顺序已在当前
> 工作树中修复；#13 中的 BUG-101（垃圾字节清空缓冲区）也已修复，只剩 BUG-100 (BodyLength=0) 仍待修。

---

## Verification Pass — 2026-07-03

> Each item below was re-verified by reading the actual source code referenced in its description.
> The verification confirmed the vast majority of findings, with corrections noted where the
> original description contained factual errors or directional mistakes.

### Summary

| Severity | Claimed | ✅ Confirmed | ⚠️ Partially confirmed | ❌ Not confirmed |
|----------|---------|-------------|------------------------|-----------------|
| P0 (BUG-23–BUG-34) | 12 | 12 | 0 | 0 |
| P1 (BUG-35–BUG-62) | 28 | 26 | 1 (BUG-36) | 1 (BUG-56) |
| P2 (BUG-63–BUG-85) | 23 | 19 | 2 (BUG-44, BUG-65) | 2 (BUG-73, plus one nuance) |
| **Total** | **63** | **57** | **3** | **3** |

### Corrections

#### BUG-36: `on_tick` heartbeat-timeout arithmetic can overflow `u32` — ⚠️ Partially confirmed

- **Overflow risk**: Confirmed. `hb * self.config.heartbeat_timeout_multiplier.max(1) + 2` is plain `u32` arithmetic; large values overflow → panic in debug, silent wrap in release.
- **`heartbeat_timeout_multiplier` is `u32`**: Confirmed. `config.rs` declares `pub heartbeat_timeout_multiplier: u32`. `builder.rs` parses it via `u32_key`. A `.cfg` value of `1.4` truncates to `1`.
- **Directional error in original description**: The text states "yielding a 2.25× shorter timeout than QFJ." This is **wrong in direction**. TrueFix's default `heartbeat_timeout_multiplier` is `2` (giving `2×HB+2`), while QFJ's default is `1.4` (giving `1.4×HB+2`). TrueFix's timeout is **longer**, not shorter. If a user sets `HeartBeatTimeoutMultiplier=1.4`, it truncates to `1`, yielding `1×HB+2` — which *is* shorter than QFJ's `1.4×HB+2`, but the original text's "2.25× shorter" figure is incorrect regardless.

#### BUG-44: Logon without `MsgSeqNum` (tag 34) is silently accepted — ⚠️ Partially confirmed

- **Core issue confirmed**: When tag 34 is absent, `logon_seq_early` is `None`, `disposition` is `None`, and the `Some(Ordering::Equal)` guard does not fire. `next_in_seq` is not advanced. The session still transitions to `LoggedOn` and a Logon response is sent (acceptor arm). The session is desynced.
- **Description nuance**: The text says "`Some(Ordering::Equal)` guard doesn't fire" — technically correct but incomplete. When `disposition` is `None`, *none* of the three match arms (`Some(Equal)`, `Some(Greater) if !resend_requested`, `_`) match except the catch-all `_ => {}`. The effect is the same but the mechanism is broader than described.

#### BUG-56: Outbound messages do not include SubID/LocationID — ❌ Not confirmed (partially)

- **`admin::base()` DOES set SubID/LocationID**: The original description claims "`admin::base()` and `send_app()` never use them." This is **factually incorrect for `base()`**. The function at `admin.rs:14-38` explicitly sets tags 50/142/57/143 when configured:
  ```rust
  if let Some(v) = &config.sender_sub_id { m.header.set(Field::string(50, v)); }
  if let Some(v) = &config.sender_location_id { m.header.set(Field::string(142, v)); }
  // ... etc
  ```
  All admin messages (Logon, Logout, Heartbeat, Reject, etc.) go through `base()` and correctly carry these fields.
- **`Session::send_app()` does NOT set them**: `state.rs` `send_app()` stamps the header manually (tags 8/49/56/34/52) without calling `base()` and without setting 50/142/57/143. So application-originated messages are indeed missing SubID/LocationID.
- **Corrected scope**: The bug exists in `send_app()` only, not in `admin::base()`. The fix should add SubID/LocationID stamping to `send_app()`.

#### BUG-65: `parse_utc_offset` integer overflow — ⚠️ Partially confirmed

- **Overflow path exists**: `sign * (hours * 3600 + minutes * 60)` uses `i32` arithmetic. `hours` is parsed as `i32`. The overflow threshold is `hours > ~596523`.
- **Practical risk is negligible**: A valid UTC offset's `hours` field is 0–14. Even a malicious `.cfg` value like `+99999:00` would parse `hours = 99999`, giving `99999 * 3600 = 359996400` which fits in `i32` (max ~2.1 billion). You'd need `hours > 596523` to overflow — an extreme, non-realistic input. The debug-panic risk is theoretical.

#### BUG-73: codegen enum-variant labels that are Rust keywords are not sanitized — ❌ Not confirmed (practically)

- **Code analysis is correct**: `sanitize_variant()` only prefixes leading digits. Enum variant labels do NOT pass through `snake_case()` (which has keyword sanitization). So a label like `Type` or `Self` would indeed produce invalid Rust.
- **Doesn't trigger in practice**: The code's own comments note "real QFJ-schema labels are always `[A-Za-z0-9_]+`" and use SCREAMING_SNAKE_CASE (e.g. `PER_UNIT`, `BUY`, `SELL`). No real QFJ enum label is a Rust keyword. The risk is theoretical for bundled dictionaries; it could only trigger on a custom dictionary source that names an enum value `Type` or `Self`.

### Per-item verification status

| BUG | Status | Notes |
|-----|--------|-------|
| BUG-23 | ✅ Confirmed | `body_start + body_len + 7` plain addition, no `checked_add` |
| BUG-24 | ✅ Confirmed | `sum::<u32>()` panics on overflow in debug |
| BUG-25 | ✅ Confirmed | `JoinHandle` never awaited, `current` stays `Some` forever |
| BUG-26 | ✅ Confirmed | INSERT before `add_creation_time_column_if_missing()` |
| BUG-27 | ✅ Confirmed | `shutdown()` skips `self.initiators`; no `Drop` impl |
| BUG-28 | ✅ Confirmed | Acceptor doesn't echo flag; initiator doesn't verify echo |
| BUG-29 | ✅ Confirmed | Too-high ResendRequest queued, not processed immediately |
| BUG-30 | ✅ Confirmed | `truncate(true)` before `write!` on every seqnum write |
| BUG-31 | ✅ Confirmed | `unwrap_or(1)` silently defaults on parse failure |
| BUG-32 | ✅ Confirmed | No check for existing active connection on same SessionID |
| BUG-33 | ✅ Confirmed | `Event` enum has no `Disconnected` variant; `enter_disconnected` never called on TCP drop |
| BUG-34 | ✅ Confirmed | `from_admin`/`from_app` called before `dispatch(Event::Received)` |
| BUG-35 | ✅ Confirmed | `on_connected()` doesn't reset `resend_requested` or `queue` |
| BUG-36 | ⚠️ Partial | Overflow confirmed; `u32` type confirmed; direction of "shorter" is wrong |
| BUG-37 | ✅ Confirmed | `decode_with_groups` has no section-ordering check |
| BUG-38 | ✅ Confirmed | `data_field_for_length(tag).is_some()` — return value discarded |
| BUG-39 | ✅ Confirmed | Plain reconnecting path missing `socket_options.apply()` |
| BUG-40 | ✅ Confirmed | `enter_disconnected` doesn't reset `logon_sent` when no reset configured |
| BUG-41 | ✅ Confirmed | COMMIT failure returns `Err` without ROLLBACK |
| BUG-42 | ✅ Confirmed | `on_received()` has no state check before processing |
| BUG-43 | ✅ Confirmed | Only handles `next_expected < next_out_seq`; too-high ignored |
| BUG-44 | ⚠️ Partial | Core issue confirmed; mechanism description incomplete |
| BUG-45 | ✅ Confirmed | `fs::read(path)` reads entire body file into `Vec<u8>` |
| BUG-46 | ✅ Confirmed | No `10=` verification at computed checksum position |
| BUG-47 | ✅ Confirmed | `s.trim().parse()` in both `as_int` and `as_decimal` |
| BUG-48 | ✅ Confirmed | Accepts ≥1 fractional digits; QFJ accepts only 0/3/6/9/12 |
| BUG-49 | ✅ Confirmed | `parse_usize` failure → `pending_data_len` not set, no error |
| BUG-50 | ✅ Confirmed | `stop()` only sets `AtomicBool`; no `control_tx` held |
| BUG-51 | ✅ Confirmed | `next_endpoint` never reset to 0 after successful connect |
| BUG-52 | ✅ Confirmed | `route_and_run` doesn't check `MsgType == "A"` |
| BUG-53 | ✅ Confirmed | `mpsc::unbounded_channel()` for admin traffic (default path) |
| BUG-54 | ✅ Confirmed | `mdef.required` checked at body level only; no recursive group check |
| BUG-55 | ✅ Confirmed | `body.fields()` skips group members in group-decoded messages |
| BUG-56 | ❌ Not confirmed | `admin::base()` DOES set 50/142/57/143; only `send_app()` is missing them |
| BUG-57 | ✅ Confirmed | `poss_dup_anti_replay_violation` only in `Ordering::Less` |
| BUG-58 | ✅ Confirmed | `reject()`/`reject_with_reason()` never set `REF_MSG_TYPE(372)` |
| BUG-59 | ✅ Confirmed | `SESSION_REJECT_REASON` set unconditionally, no version filtering |
| BUG-60 | ✅ Confirmed | `on_logout_msg()` passes `None` for text |
| BUG-61 | ✅ Confirmed | `Services` from `members.first()`; per-member store via `with_session_store` but log/validator/socket_options from primary |
| BUG-62 | ✅ Confirmed | `Char` arm calls `as_char()` unconditionally for all versions |
| BUG-63 | ✅ Confirmed | `precision_key` requires exact uppercase |
| BUG-64 | ✅ Confirmed | `bool_key` treats any non-"Y" as `false` |
| BUG-65 | ⚠️ Partial | Overflow path exists but requires extreme input (>596523 hours) |
| BUG-66 | ✅ Confirmed | `prepare_resend` uses `now_utc_timestamp()` (hardcoded millis) |
| BUG-67 | ✅ Confirmed | `hbi as u32` truncates `i64` |
| BUG-68 | ✅ Confirmed | `Ordering::Less` non-PossDup path has no `reset_on_error()` |
| BUG-69 | ✅ Confirmed | `AwaitingLogout` (not timed out) falls through to `_ => {}` |
| BUG-70 | ✅ Confirmed | `split_once('@')` splits at first `@`; password with `@` mis-split |
| BUG-71 | ✅ Confirmed | `spawn_blocking` thread keeps running on timeout; socket leaked |
| BUG-72 | ✅ Confirmed | `type_mapping` omits `TIME`/`PRICEOFFSET`/etc.; falls to `&str` |
| BUG-73 | ❌ Not confirmed (practical) | Code analysis correct but QFJ enum labels are SCREAMING_SNAKE_CASE, never Rust keywords |
| BUG-74 | ✅ Confirmed | Runtime parser strips `open` token; codegen's `parse_dict` ingests it as enum value |
| BUG-75 | ✅ Confirmed | Disposition/seq logic runs before role/state guards; `_ => {}` no-ops response but side effects already applied |
| BUG-76 | ✅ Confirmed | TLS from `find_map`; Services from `first()` — can come from different members |
| BUG-77 | ✅ Confirmed | `Services::default()` → `monitor: None`; `Engine::start` never sets it |
| BUG-78 | ✅ Confirmed | `Time::from_hms_nano` rejects sec=60; no leap-second mapping |
| BUG-79 | ✅ Confirmed | Only checks `buf.get(0..2) == Some(b"8=")`; no `FIX.X.X` pattern |
| BUG-80 | ✅ Confirmed | `tokenize_validated` requires tags 8/9/10 but not 35; `9=0` accepted |
| BUG-81 | ✅ Confirmed | `body.all()` reads all message bodies before cache insertion |
| BUG-82 | ✅ Confirmed | `mark_disconnected` sets `connected: false` but doesn't update `status` |
| BUG-83 | ✅ Confirmed | `sequence_reset()` sets PossDupFlag but not OrigSendingTime |
| BUG-84 | ✅ Confirmed | Uses `config.timestamp_precision` regardless of FIX version; no < 4.2 seconds-only enforcement |
| BUG-85 | ✅ Confirmed | `AwaitingLogon` timeout applies to both roles; QFJ only times out initiator |

---

# QFJ + QFGo Comparison Findings — BUG-86 onwards

> Generated 2026-07-03 by a systematic source-level comparison of TrueFix (`crates/`) against:
> - **QFJ** (`thrdpty/quickfixj/quickfixj-core/src/main/java/quickfix/`) — `Session.java` (3167 lines),
>   `AcceptorIoHandler.java`, `FileStore.java`, `Responder.java`, `FIXMessageDecoder.java`
> - **QFGo** (`thrdpty/quickfix/*.go`) — `session.go`, `in_session.go`, `logon_state.go`,
>   `session_state.go`, `resend_state.go`, `validation.go`, `parser.go`,
>   `store/file/file_store.go`
>
> Items already tracked in `003.md` or earlier in this document (BUG-23–BUG-85) are **not** repeated.
> Numbering continues: `BUG-86` onwards.
>
> **Verification**: Each item was verified against QFJ source with specific line citations.
> Items marked ✅ are confirmed bugs; items marked ⚠️ have nuances.

---

## P0 — Critical (QFJ/QFGo comparison)

### BUG-86: Session state machine has no schedule awareness — mid-connection schedule violations go unenforced

**TF**: `crates/truefix-session/src/state.rs`, `crates/truefix-transport/src/lib.rs`.
**QFJ**: `Session.java` `next()` L1961-1976. **QFGo**: `session_state.go` `CheckSessionTime()`.

QFJ's `Session.next()` checks `isSessionTime()` every second and resets/disconnects if outside the
schedule window — for both initiator and acceptor. `resetIfSessionNotCurrent` (L3139) compares
`store.getCreationTimeCalendar()` against `sessionSchedule.isSameSession()`. QFGo calls
`CheckSessionTime` on **every incoming message** in addition to the per-second ticker.

TrueFix's schedule logic lives only in `transport/lib.rs` `run_scheduled_initiator` — the acceptor
has zero schedule awareness. The session state machine (`state.rs`) has no concept of schedule at all.

**Impact**: An acceptor that should only be active during trading hours will accept Logons at 3am
and keep the session alive. A daily-reset schedule is silently ignored on the acceptor side. ✅ **Confirmed**

### BUG-87: Acceptor does not reject Logon outside session schedule

**TF**: `crates/truefix-session/src/state.rs` `on_logon()`.
**QFJ**: `Session.java` `nextLogon()` L2182-2183. **QFGo**: `session.go` `onAdmin()`.

QFJ: `if (!isSessionTime()) { throw new RejectLogon("Logon attempt not within session time"); }`
QFGo: `if !s.IsSessionTime() { return errors.New("Connection outside of session time") }`

TrueFix's `on_logon()` has no schedule check. ✅ **Confirmed**

### BUG-88: `forceResendWhenCorruptedStore` semantics diverge — admin messages not resent during gap-fill

**TF**: `crates/truefix-session/src/state.rs` `build_resend()`.
**QFJ**: `Session.java` `resendMessages()` L2386-2392.

QFJ's `forceResendWhenCorruptedStore` has TWO effects: (1) admin messages are resent instead of
gap-filled when the flag is true; (2) on `IOException` reading from store, heartbeats are sent
instead of throwing. TrueFix only uses this flag at store-open time. ✅ **Confirmed**

---

## P1 — High/Medium (QFJ/QFGo comparison)

### BUG-89: Dictionary validation skipped for all admin messages

**TF**: `crates/truefix-session/src/state.rs` `validate_app()`.
**QFJ**: `Session.java` `next()` L1057-1075. **QFGo**: `validation.go` `validateFIX()`.

QFJ validates ALL messages (admin and application) against the DataDictionary before dispatching.
QFGo likewise. TrueFix's `validate_app()` explicitly skips admin: `if is_admin_type(msg.msg_type()) { return None; }`.
✅ **Confirmed**

### BUG-90: `on_logon` does not disconnect when receiving Logon response before sending Logon

**TF**: `crates/truefix-session/src/state.rs` `on_logon()`.
**QFJ**: `Session.java` `nextLogon()` L2253-2255. **QFGo**: `logon_state.go` `FixMsgIn()`.

QFJ: `if (state.isLogonSendNeeded() && !state.isResetReceived()) { disconnect("Received logon response before sending request", true); }`
TrueFix silently ignores unsolicited Logons via `_ => {}`. ✅ **Confirmed**

### BUG-91: `sendRaw` does not dynamically detect `ResetSeqNumFlag` on outgoing Logon

**TF**: `crates/truefix-session/src/state.rs` `on_connected()`.
**QFJ**: `Session.java` `sendRaw()` L2690-2710. **QFGo**: `session.go` `prepMessageForSend()` L325-345.

Both QFJ and QFGo check the outgoing Logon message itself for `ResetSeqNumFlag` after the `toAdmin`
callback. TrueFix only checks `config.reset_on_logon` (a static config) in `on_connected()`. If
`toAdmin` sets `ResetSeqNumFlag=Y`, TrueFix won't detect it and won't reset. ✅ **Confirmed**

### BUG-92: QFJ/QFGo initiator sends `ResetSeqNumFlag` on fresh start; TrueFix only when `reset_on_logon` is configured — ⚠️ Partially confirmed

**TF**: `crates/truefix-session/src/state.rs` `on_connected()`.
**QFJ**: `Session.java` `isResetNeeded()` L919-922. **QFGo**: `session.go` `shouldSendReset()`.

QFJ: `isResetNeeded()` returns `true` when `(resetOnLogon || resetOnLogout || resetOnDisconnect) && both seqs == 1`.
QFGo: `shouldSendReset()` checks the same three configs AND both seqs == 1.

**Correction**: QFJ does NOT send the flag on *every* fresh start — it requires at least one reset
config to be true. But QFJ checks all three configs; TrueFix only checks `reset_on_logon`. Also
TrueFix doesn't check that seqs == 1 (unconditionally resets when `reset_on_logon=true`). TrueFix's
`reset_on_logon` defaults to `true`, while QFJ's `resetOnLogon` defaults to `false`.

### BUG-93: `disconnect()` calls `onLogout` only if `logged_on`; QFJ/QFGo call if `logonSent || logonReceived`

**TF**: `crates/truefix-transport/src/lib.rs` `run_connection` shutdown path.
**QFJ**: `Session.java` `disconnect()` L2129-2150. **QFGo**: `session_state.go` `handleDisconnectState()`.

QFJ: `if (logonReceived || logonSent) { application.onLogout(sessionID); }`
QFGo: calls `onLogout` when `IsLoggedOn()` OR in `logoutState` OR in `logonState` with `InitiateLogon`.
TrueFix only calls `onLogout` when `logged_on` (state == LoggedOn). ✅ **Confirmed**

### BUG-94: No logon retry backoff — scheduled initiator polls every 200ms

**TF**: `crates/truefix-transport/src/lib.rs` `run_scheduled_initiator`.
**QFJ**: `Session.java` `computeNextLogonDelayMillis()` L2049-2062 — `logonIntervals = {2, 5, 10, 15, 30}` seconds.

TrueFix's `run_scheduled_initiator` sleeps 200ms between iterations. QFJ backs off to 2s→5s→10s→15s→30s.
✅ **Confirmed**

### BUG-95: `on_logon` does not reject `HeartBtInt < 0` — silently uses default

**TF**: `crates/truefix-session/src/state.rs` `on_logon()`.
**QFJ**: `Session.java` `next()` L1049-1050.

QFJ: `if (message.getInt(HeartBtInt.FIELD) < 0) { throw new RejectLogon("HeartBtInt must not be negative"); }`
TrueFix's `hbi > 0` filter silently ignores negative values. ✅ **Confirmed**

### BUG-96: `on_sequence_reset` (gap-fill mode) does not dequeue skipped messages

**TF**: `crates/truefix-session/src/state.rs` `on_sequence_reset()`.
**QFJ**: `Session.java` `nextSequenceReset()` L1575-1577 (QFJ-728).

QFJ: `state.getMessageQueue().dequeueMessagesUpTo(newSequence);`
TrueFix's `drain_queue` only removes messages matching `next_in_seq` — messages below `ns` that
were queued remain in the `BTreeMap` forever — memory leak. ✅ **Confirmed**

### BUG-97: ResendRequest `EndSeqNo=999999` not treated as "infinity" for FIX ≤ 4.1

**TF**: `crates/truefix-session/src/state.rs` `on_resend_request()`.
**QFJ**: `Session.java` `manageGapFill()` L1367, `sendResendRequest()` L2563. **QFGo**: `session.go` `sendResendRequest()`.

Both QFJ and QFGo use `999999` as "infinity" for FIX ≤ 4.1. TrueFix only checks `end_req == 0`.
In practice usually accidentally correct (since `999999 > last`), but intent not honored. ✅ **Confirmed**

### BUG-98: `handleLogon` resets store BEFORE verifying the Logon message

**TF**: `crates/truefix-session/src/state.rs` `on_logon()`.
**QFJ**: `Session.java` `nextLogon()` L2200-2230. **QFGo**: `session.go` `handleLogon()`.

QFJ verifies the Logon (CompID, latency, etc.) via `verify(logon, false, ...)` BEFORE calling
`resetState()`. QFGo calls `verifyMsgAgainstAppImpl(msg)` BEFORE `store.Reset()`. TrueFix's `on_logon`
calls `self.reset_sequences(full)` when `ResetSeqNumFlag=Y` BEFORE any identity/latency check runs.
A malformed Logon with `ResetSeqNumFlag=Y` but invalid CompIDs causes data loss before rejection.
✅ **Confirmed**

### BUG-99: `FileStore.reset()` truncates files in place vs QFJ/QFGo's delete-and-recreate

**TF**: `crates/truefix-store/src/file.rs` `SeqFile::write`, `BodyLog::reset`.
**QFJ**: `FileStore.java` `reset()` → `initialize(true)` → `closeAndDeleteFiles()`. **QFGo**: `store/file/file_store.go` `Reset()`.

Both QFJ and QFGo close all files, delete them, and create new ones. Both use **separate**
`senderseqnums` and `targetseqnums` files. TrueFix truncates files in place and uses one combined
`seqnums` file. ✅ **Confirmed**

### BUG-100: Parser does not validate `BodyLength > 0` — zero BodyLength accepted — ⚠️ Confirmed against BOTH QFJ and QFGo

**TF**: `crates/truefix-core/src/framing.rs` `frame_length()`.
**QFJ**: `FIXMessageDecoder.java` `parseMessage()` — rejects `bodyLength == 0` (QFJ-903).
**QFGo**: `parser.go` `jumpLength()` — rejects `length <= 0`.

TrueFix's `frame_length` accepts `body_len == 0` and returns `Ok(Some(total))`. **Confirmed bug against both references.**

### BUG-101: Parser does not skip garbage bytes before `8=` — single bad byte kills the connection

**TF**: `crates/truefix-core/src/framing.rs` `frame_length()`, `crates/truefix-transport/src/lib.rs` `classify_buffered()`.
**QFJ**: `FIXMessageDecoder.java` — `SEEKING_HEADER` state scans forward; `MAX_UNDECODED_DATA_LENGTH = 4096`.
**QFGo**: `parser.go` `findStart()` — scans forward to find `8=`.

Both QFJ and QFGo scan forward to find `8=`. TrueFix's `frame_length()` returns `Err` and
`classify_buffered` does `buf.clear()` — discarding the entire buffer. QFJ also has a **max garbage
limit** (4KB) that TrueFix lacks. ✅ **Confirmed**

### BUG-102: QFGo `EnableResetSeqTime` is fully implemented; TrueFix has no equivalent

**TF**: no equivalent (GAP-11 in 003.md).
**QFGo**: `session_state.go` `CheckResetTime()` — sends `ResetSeqNumFlag=Y` Logon at a configured
daily time, mid-connection, without disconnecting. ✅ **Confirmed** (QFGo-only feature gap)

### BUG-103: QFGo `resendMessages` uses streaming `IterateMessages`; TrueFix materializes all messages in memory

**TF**: `crates/truefix-session/src/state.rs` `build_resend()` — iterates in-memory `BTreeMap`.
**QFGo**: `store/file/file_store.go` `IterateMessages()` — streaming callback via `ReadAt`.
**QFJ**: `FileStore.java` `get()` — reads on-demand via `RandomAccessFile.seek()`.

QFGo's `IterateMessages` reads each message body from disk on demand. QFJ's `FileStore.get()`
also reads on demand via `RandomAccessFile.seek()`. TrueFix's in-memory `BTreeMap` grows unbounded
for the session's lifetime. ✅ **Confirmed**

---

## P2 — Low (QFJ/QFGo comparison)

### BUG-104: `next()` does not initiate logout when session is disabled

**TF**: `crates/truefix-session/src/state.rs` `on_tick()`.
**QFJ**: `Session.java` `next()` L2018-2024. **QFGo**: `session.go` `stop()` → `loggedOn.Stop()` → `initiateLogout`.

QFJ: `if (!isEnabled() && isLoggedOn()) { generateLogout(state.getLogoutReason()); }`
TrueFix has no concept of session "enabled/disabled" in the session state machine. ✅ **Confirmed**

### BUG-105: `generateReject` refuses to send Reject before logon — TrueFix only guards `on_garbled`

**TF**: `crates/truefix-session/src/state.rs` `validate_app()`, `on_garbled()`.
**QFJ**: `Session.java` `generateReject()` L1640. **QFGo**: `in_session.go` `processReject()` in `logonState`.

QFJ: `if (!state.isLogonReceived()) { throw new SessionException("Tried to send a reject while not logged on"); }`
TrueFix's `on_garbled()` checks state, but `validate_app()` has no such guard. ✅ **Confirmed**

### BUG-106: QFGo `generateSequenceReset` sets `OrigSendingTime`; TrueFix does not

**TF**: `crates/truefix-session/src/admin.rs` `sequence_reset()`.
**QFJ**: `Session.java` `generateSequenceReset()` L1392-1393. **QFGo**: `in_session.go` `generateSequenceReset()`.

Both QFJ and QFGo set `OrigSendingTime = SendingTime` on gap-fill SequenceReset. TrueFix does not.
(Already tracked as BUG-83, confirmed independently.) ✅ **Confirmed**

### BUG-107: QFGo aligns ticker to round second — TrueFix does not

**TF**: `crates/truefix-transport/src/lib.rs` `run_connection` — `interval(Duration::from_secs(1))` starts immediately.
**QFGo**: `session.go` `run()` — `time.Sleep(time.Until(time.Now().Truncate(time.Second).Add(time.Second)))`.

QFGo sleeps until the next round second before starting the ticker. Schedule boundary checks could
miss by up to 999ms in TrueFix. ✅ **Confirmed** (QFGo-only)

### BUG-108: QFGo peer timeout is `1.2 × HeartBtInt`; TrueFix uses configurable multiplier defaulting to `2` — ⚠️ Partially confirmed

**TF**: `crates/truefix-session/src/state.rs` `on_tick()` — `hb * heartbeat_timeout_multiplier.max(1) + 2`.
**QFJ**: configurable multiplier, default 1.4, stored as `double`. **QFGo**: hardcoded 1.2.

TrueFix default of 2 is more lenient than both QFJ (1.4) and QFGo (1.2): `2×HB+2` vs `1.4×HB+2` vs `1.2×HB`.

### BUG-109: QFGo `shouldSendReset` checks `ResetOnDisconnect` and `ResetOnLogout` in addition to `ResetOnLogon`

**TF**: `crates/truefix-session/src/state.rs` `on_connected()` — only checks `config.reset_on_logon`.
**QFJ**: `Session.java` `isResetNeeded()` L919-922. **QFGo**: `session.go` `shouldSendReset()`.

Both QFJ and QFGo check all three reset configs. TrueFix only checks `reset_on_logon`. ✅ **Confirmed**

### BUG-110: `closed_resend_interval` is a no-op

**TF**: `crates/truefix-session/src/config.rs` `closed_resend_interval`.
**QFJ**: `Session.java` `useClosedRangeForResend` L2557-2565. **QFGo**: `session.go` `sendResendRequest()`.

Both QFJ and QFGo have functional closed-range behavior. TrueFix's `closed_resend_interval` is
documented as "accepted but intentionally a no-op." ✅ **Confirmed**

### BUG-111: QFJ/QFGo `Session` persists across connections; TrueFix creates new Session per connection

**TF**: `crates/truefix-transport/src/lib.rs` `run_connection()`.
**QFJ**: `Session.java` (class-level Javadoc). **QFGo**: `session.go`.

Both QFJ and QFGo persist the Session object across connections. This enables duplicate connection
detection, state retention, `validLogonState` enforcement. TrueFix creates a new `Session::new()`
each time. ✅ **Confirmed** (architectural context for BUG-32/BUG-33/BUG-42/BUG-93)

---

## QFJ Verification Summary

| BUG | Status | QFJ Source |
|-----|--------|-----------|
| BUG-86 | ✅ | `Session.java` `next()` L1961-1976 |
| BUG-87 | ✅ | `Session.java` `nextLogon()` L2182-2183 |
| BUG-88 | ✅ | `Session.java` `resendMessages()` L2386-2392 |
| BUG-89 | ✅ | `Session.java` `next()` L1057-1075 |
| BUG-90 | ✅ | `Session.java` `nextLogon()` L2253-2255 |
| BUG-91 | ✅ | `Session.java` `sendRaw()` L2690-2710 |
| BUG-92 | ⚠️ | `Session.java` `isResetNeeded()` L919-922 — QFJ requires reset config AND seqs==1 |
| BUG-93 | ✅ | `Session.java` `disconnect()` L2129-2150 |
| BUG-94 | ✅ | `Session.java` `computeNextLogonDelayMillis()` L2049-2062 |
| BUG-95 | ✅ | `Session.java` `next()` L1049-1050 |
| BUG-96 | ✅ | `Session.java` `nextSequenceReset()` L1575-1577 |
| BUG-97 | ✅ | `Session.java` `manageGapFill()` L1367, `sendResendRequest()` L2563 |
| BUG-98 | ✅ | `Session.java` `nextLogon()` L2200-2230 |
| BUG-99 | ✅ | `FileStore.java` `reset()` → `closeAndDeleteFiles()` |
| BUG-100 | ✅ | `FIXMessageDecoder.java` QFJ-903 rejects `bodyLength==0`; QFGo rejects `<=0` |
| BUG-101 | ✅ | `FIXMessageDecoder.java` `SEEKING_HEADER` scans forward; `MAX_UNDECODED_DATA_LENGTH=4096` |
| BUG-102 | ✅ | QFGo-only: `session_state.go` `CheckResetTime()` |
| BUG-103 | ✅ | QFGo `IterateMessages` streaming; QFJ `FileStore.get()` reads on-demand |
| BUG-104 | ✅ | `Session.java` `next()` L2018-2024 |
| BUG-105 | ✅ | `Session.java` `generateReject()` L1640 |
| BUG-106 | ✅ | `Session.java` `generateSequenceReset()` L1392-1393 |
| BUG-107 | ✅ | QFGo-only: `session.go` `run()` |
| BUG-108 | ⚠️ | QFJ default 1.4 (`double`); QFGo 1.2; TrueFix 2 (`u32`) |
| BUG-109 | ✅ | `Session.java` `isResetNeeded()` L919-922 |
| BUG-110 | ✅ | `Session.java` `useClosedRangeForResend` L2557-2565 |
| BUG-111 | ✅ | `Session.java` class Javadoc; QFGo `session.go` |

| Severity | Count | ✅ Confirmed | ⚠️ Partially | ❌ Not confirmed |
|----------|-------|-------------|---------------|-----------------|
| P0 (BUG-86–BUG-88) | 3 | 3 | 0 | 0 |
| P1 (BUG-89–BUG-103) | 15 | 13 | 2 (BUG-92, BUG-100) | 0 |
| P2 (BUG-104–BUG-111) | 8 | 7 | 1 (BUG-108) | 0 |
| **Total** | **26** | **23** | **3** | **0** |

---

## 二次交叉验证 — 2026-07-03（第二轮，四路并行独立核对）

> 本轮由 4 个独立 agent 并行执行，覆盖 (1) codec/framing/store, (2) session 状态机,
> (3) transport/engine 生命周期, (4) dict/config/admin message 四个子系统，逐条重新阅读
> **当前工作树**源码（含 006-audit-remediation 尚未提交的 WIP 改动）与
> `thrdpty/quickfixj`、`thrdpty/quickfix` 对应源码，不采信本文档此前"Verification Pass"
> 的结论，独立复核。核对了 BUG-23~BUG-111 全部 93 项（含 BUG-86~111）。
>
> 结论：绝大多数发现（85+ 项）核实为真、且在当前代码中仍然存在。以下是需要更正的部分。

### A. 已在当前工作树（006 WIP）中修复 —— 应从待办中移除，避免重复修

| BUG | 说明 |
|-----|------|
| **BUG-37** | `decode_with_groups` 现在通过 `section_of` 显式设置 `fields_out_of_order`，代码注释标注 "GAP-26/FR-032 (feature 006)"。原描述的 gap 已不存在。 |
| **BUG-98** | `on_logon()` 中 CompID/latency 校验（含 BUG-05 修复引入的过低 MsgSeqNum 检查）现在确实排在 `reset_sequences()` 之前执行。原描述的"先 reset 后 verify"顺序已被 006 对 BUG-05 的修复顺带纠正。 |
| **BUG-101** | `classify_buffered()` 的 `Err` 分支现在调用 `next_frame_start()` 向前扫描寻找 `8=`，只丢弃畸形前缀，不再 `buf.clear()` 清空整个缓冲区。代码注释标注 "B14/FR-028 (feature 006)"。（仍缺 QFJ 式的 4KB 垃圾上限，但核心指控——"一个坏字节导致整个缓冲区被清空"——已不成立。） |

### B. 已部分修复 —— 需要缩小范围重新描述

| BUG | 修正后的范围 |
|-----|------|
| **BUG-23** | `frame_length()` 现在有 `MAX_BODY_LEN`（16MiB）上限校验，发生在 `body_start+body_len+7` 加法**之前**——该溢出路径已不可达。仍未使用 `checked_add`，但已不是"远程可利用的 DoS"，只是缺少纵深防御。建议改为 P2/低优先级"补充 checked_add 作为防御性加固"，而非"必须修复的远程可利用溢出"。 |
| **BUG-61** | Store 现在已经按 member 各自构建（`with_session_store`），不再是"仅从 members.first() 构建"。但 `log`/`validator`/`socket_options`/`trusted_proxy_addresses`/`sync_write_timeout`/TLS 仍然只从 `members.first()`（或 TLS 的 `find_map` 第一个匹配项）取，跨 session 共享端口时这几项仍可能来自错误的 member。范围应缩小为"log/validator/socket_options/TLS 未按 member 隔离"，store 部分已解决。 |
| **BUG-27** | `Engine::shutdown()` 主路径确实仍不处理 `self.initiators`、仍无 `Drop`；但 006 WIP 新增的 `cleanup_partial_start` 已经处理了"启动失败"路径上的 initiator 清理。核心 gap（正常运行时无法从 API 停止 initiator）仍然成立，但不是完全没有任何清理机制。 |

### C. 原文档描述本身有误（其自身"Verification Pass"未发现的新问题）

| BUG | 错误之处 | 更正 |
|-----|---------|------|
| **BUG-102** | 声称"TrueFix 无 `EnableResetSeqTime` 等价物" | ❌ 错误。`crates/truefix-session/src/schedule_reset.rs` 的 `decide_recurring_reset()` 已完整实现了等价逻辑（含测试），只是没有被 `truefix-transport`/`Engine` 调用——是"实现了但未接线"的 dormant 问题（与 GAP-26/27 同一类别），不是"未实现"。应更正措辞为"功能已实现但未被运行时路径调用"。 |
| **Exchange 大小写**（"Partially covered" 表） | 声称 QFJ/QFGo 要求大写、TrueFix 用 `is_ascii_alphanumeric()` 更宽松是个需要关注的差异 | ❌ 错误。QFJ 的 `checkValidFormat`（`DataDictionary.java`）和 QFGo 的 `validation.go` 均将 EXCHANGE 当作**未经校验的普通字符串**处理，两个参考实现都没有大小写要求。这条对两个参考实现的行为描述都不准确，应整条删除。 |
| **BUG-60** | 声称"QFJ 会 echo 收到 Logout 的 Text(58)" | ⚠️ 部分错误。QFJ 的 `generateLogout(otherLogout)` 同样传 `text=null`，**并不回显 Text(58)**——这半个指控对 QFJ 的描述不准确。`LastMsgSeqNumProcessed` 部分确有问题，但更准确的描述是"TrueFix 的 369 标记基于 `next_in_seq-1`，未把当前 Logout 自身的 seq 计入"（一个 off-by-one，而非"完全没有"）。 |
| **BUG-90** | 描述的具体竞争场景（"在发送 Logon 前收到 Logon 响应"）适用于 TrueFix | ⚠️ 有 nuance。该场景在 TrueFix 架构下实际不会发生：`on_connected()` 中 Initiator 的 Logon 是同步发送、发送完成后才把状态置为 `AwaitingLogon`，所以状态到达 `AwaitingLogon` 时 `logon_sent` 必然已为 `true`。真实存在的是一个相邻但不同的 gap："`AwaitingLogout`/`Disconnected` 状态下收到 stray Logon 被 `_ => {}` 静默吞掉"，应该以此重新描述。 |
| **BUG-97** | 引用的版本阈值为"FIX ≤ 4.1" | ⚠️ 版本号错误。QFJ (`manageGapFill()`) 和 QFGo 都是以 **FIX ≤ 4.2** 作为 999999-视为-infinity 的判断阈值，不是 4.1。结论本身（TrueFix 只判断 `end_req==0`）仍然成立，仅需更正引用的版本号。 |
| **BUG-24** | 笼统地说"checksum sum::<u32>() 溢出" | 需要拆分：production 唯一调用路径 `frame_length→decode` 现因 BUG-23 引入的 `MAX_BODY_LEN` 间接封顶，`decode.rs` 侧实际不会溢出。但 `encode.rs:55` 的 `sum::<u32>()` **没有任何上限**——应用层构造一条 RawData 超过 ~16.7MB 的消息仍会在 encode 侧溢出（debug panic / release 静默 wrap 出错误 checksum）。应将范围缩小为"仅 encode 侧仍有溢出风险"。 |
| **BUG-30** | 将"truncate-then-write"直接定性为需要修复的 bug，对标 QFJ 的 seek+writeUTF 不截断 | 需要补充设计背景：QFJ 的 `writeUTF` 自带长度前缀，不截断也不会残留脏数据；TrueFix 用纯文本格式，若照搬"不截断"反而会因为新值变短而在文件尾部残留旧字符、导致解析出错。这更像是格式设计差异而非"抄错了"，修复时应保留截断语义、只改变截断与写入的原子性（如先写临时文件再 rename），而不是简单去掉 truncate。 |

### 二次核实统计

| 类别 | 数量 |
|------|------|
| 核实为真、现状不变 | 85 |
| 已在当前工作树修复（应移除） | 3（BUG-37, BUG-98, BUG-101） |
| 范围需要缩小 | 3（BUG-23, BUG-61, BUG-27） |
| 描述本身有误需更正 | 6（BUG-102, Exchange 大小写, BUG-60, BUG-90, BUG-97, BUG-24） |
| 设计背景需要补充（不改变结论） | 1（BUG-30） |

---

# 生产发布评估 — 2026-07-03

> 基于 BUG-23 ~ BUG-111 共 89 项，逐一对照当前 TrueFix 源码和 `thrdpty/quickfixj` +
> `thrdpty/quickfix` 参考实现，评估发布生产的修改必要性。

## 总览

| 分类 | 数量 | 说明 |
|------|------|------|
| 🔴 必须修复才能上线 | 15 | 数据丢失、协议破坏、远程可利用、资源泄漏（原 18 项，二次核实后 BUG-23/98/101 移出，见下方"二次核实"批注） |
| 🟡 建议修复但可带病上线 | 31 | 真实缺陷但影响范围窄或需特殊条件触发 |
| 🟢 可延后处理 | 33 | 低概率、工具链问题、理论性风险（BUG-37 移出、BUG-23 移入） |
| ⚪ 非问题/已修正 | 10 | 描述有误或已非实际 bug（新增 BUG-37/98/101 —— 已在 006 WIP 中修复；详见下方"二次交叉验证"章节） |
| **合计** | **89** | |

---

## 🔴 必须修复才能上线 (P0-Block)

### 数据丢失 / 持久化

| BUG | 问题 | 为什么必须修 | 修复难度 |
|-----|------|-------------|---------|
| **BUG-30** | SeqFile truncate-then-write | 每条消息都先清空 seqnums 文件再写。崩溃即丢双侧序号 | 低 |
| **BUG-31** | 损坏 seqnums 静默默认 (1,1) | 配合 BUG-30，崩溃后无声恢复到 (1,1)，无日志 | 低 |
| **BUG-26** | SQL Store 迁移顺序错误 | 已有数据库升级时 INSERT 引用不存在的列 → 部署阻塞 | 低 |
| **BUG-41** | MSSQL COMMIT 失败不 ROLLBACK | 留下未关闭事务在 mutex 连接上，后续所有操作阻塞 | 低 |

> ⚠️ **二次核实（2026-07-03 第二轮）**：BUG-23 和 BUG-98 已移出此表。BUG-23 的溢出路径现被
> `frame_length` 中新引入的 `MAX_BODY_LEN`（16MiB）上限校验挡在加法之前，已不可达——降级为 🟢
> "建议补充 `checked_add` 作为纵深防御"，不再是上线阻塞项。BUG-98 的 reset/verify 顺序已被 006
> 对 BUG-05 的修复顺带纠正（verify 现在确实排在 reset 之前）——移至 ⚪ 已修复。

### 协议正确性

| BUG | 问题 | 为什么必须修 | 修复难度 |
|-----|------|-------------|---------|
| **BUG-28** | ResetSeqNumFlag 握手完全失效 | 序号重置机制不可用，initiator/acceptor 无法对齐序号 | 中 |
| **BUG-29** | 过高序列号 ResendRequest 死锁 | 特定序列可导致双向死锁 | 中 |
| **BUG-34** | `from_app`/`from_admin` 在 session 验证之前调用 | 应用收到未验证消息，可能处理了即将被拒绝的消息 | 中 |
| **BUG-89** | Admin 消息跳过字典校验 | 畸形 admin 消息（如 Logon 含非法字段）不经校验直接处理 | 低 |

### 连接生命周期 / 资源管理

| BUG | 问题 | 为什么必须修 | 修复难度 |
|-----|------|-------------|---------|
| **BUG-25** | Scheduled initiator 断连后永不重连 | 代码注释承诺自动重连但实际不工作 | 低 |
| **BUG-27** | `Engine::shutdown()` 无法停止普通 initiator | 资源泄漏：acceptor 继续接受连接、initiator 永远运行 | 中 |
| **BUG-33** | TCP 断连时 `reset_on_disconnect` 被忽略 | 配置项静默无效，store 保留过期序号 | 中 |
| **BUG-32** | 无重复连接保护 | 第二个相同 SessionID 的连接创建竞争 session → 序号损坏 | 中 |

### 安全 / DoS

| BUG | 问题 | 为什么必须修 | 修复难度 |
|-----|------|-------------|---------|
| **BUG-100** | Parser 接受 `BodyLength=0` | QFJ (QFJ-903) 和 QFGo 都拒绝。TrueFix 接受空消息体 | 极低 |

> ⚠️ **二次核实**：BUG-101 已移出此表——`classify_buffered` 的 `Err` 分支现在调用
> `next_frame_start` 向前扫描寻找 `8=`，只丢弃畸形前缀，不再整体清空缓冲区（006 WIP，标注
> "B14/FR-028"）。移至 ⚪ 已修复。（仍缺 QFJ 式 4KB 垃圾上限，可作为 🟢 级后续加固项。）

### 调度 / 运维

| BUG | 问题 | 为什么必须修 | 修复难度 |
|-----|------|-------------|---------|
| **BUG-86** | Acceptor 无 schedule 感知 | 交易时段外接受 Logon 并维持会话。QFJ/QFGo 都在 session 层强制 | 中 |
| **BUG-87** | Acceptor 不拒绝非交易时段 Logon | 同上的直接表现 | 低 |

---

## 🟡 建议修复但可带病上线 (Should-Fix)

这些是真实 bug，但在以下条件之一下影响范围有限：需要特殊配置才触发（如 `reset_on_logon=false`）、需要特定的对端行为、影响的是运维便利性而非正确性、有 workaround。

| BUG | 问题 | 带病上线的风险 | 建议优先级 |
|-----|------|-------------|-----------|
| **BUG-24** | Checksum `u32` 溢出 debug panic | `decode.rs` 侧因 BUG-23 的 `MAX_BODY_LEN` 上限已间接封顶，不会溢出；**`encode.rs:55` 的 `sum::<u32>()` 仍无任何上限**——应用层构造 >~16.7MB 的 RawData 消息会在 encode 侧溢出（debug panic / release 静默出错 checksum）。范围缩小为"仅 encode 侧" | 低 |
| **BUG-35** | `resend_requested` 重连后未重置 | 重连后新 gap 不触发 ResendRequest。需要重连+新 gap 同时出现 | 中 |
| **BUG-39** | Plain reconnecting initiator 跳过 socket options | `tcp_no_delay` 等未设置 → 延迟增加。不影响正确性 | 中 |
| **BUG-40** | `logon_sent` AwaitingLogon 超时后未重置 | 重连后 `seed_sequences` 被阻止。需要 `reset_on_disconnect=false` + 超时 + 重连 | 中 |
| **BUG-42** | 无 `validLogonState` 检查 | Logon 前收到非 Logon 消息会被处理。需要对端在 Logon 前发其他消息 | 中 |
| **BUG-43** | `NextExpectedMsgSeqNum(789)` 过高未拒绝 | 对端期望我们没发过的消息。静默 desync | 中 |
| **BUG-44** | 无 MsgSeqNum 的 Logon 被接受 | 需要对端发送无 tag 34 的 Logon。session desync | 中 |
| **BUG-46** | `frame_length` 不验证 CheckSum 位置 | 错误 BodyLength 导致 drain 错误字节数。需要畸形 BodyLength | 中 |
| **BUG-50** | `ReconnectHandle::stop()` 无法停止已连接 initiator | 运维不便但非正确性问题 | 低 |
| **BUG-52** | Acceptor 不验证首条消息是 Logon | 非 Logon 首消息被路由。需对端发送非 Logon 首消息 | 中 |
| **BUG-53** | Unbounded admin channel | 慢 `from_app` 导致 admin channel 无限增长。默认 `in_chan_capacity=None` 时 | 中 |
| **BUG-57** | PossDup anti-replay 仅 `Ordering::Less` 执行 | `PossDupFlag=Y` 且 seq==expected 的 replay 不被检查。需要特定 PossDup 消息 | 中 |
| **BUG-58** | Reject 缺 `RefMsgType(372)` | FIX 4.2+ 协议违规。严格对端可能拒绝 | 中 |
| **BUG-59** | `SessionRejectReason(373)` 未版本过滤 | FIX 4.0/4.1 协议违规 | 低 |
| **BUG-60** | Logout 不 echo `LastMsgSeqNumProcessed` | 二次核实：QFJ 的 `generateLogout` 同样不 echo `Text(58)`（该半个指控对 QFJ 描述有误，已从描述中删除）；真实问题是 369 标记基于 `next_in_seq-1`，未计入当前 Logout 自身 seq 的 off-by-one | 低 |
| **BUG-61** | Acceptor-group log/validator/socket_options/TLS 仅从第一个 member 构建 | 二次核实：store 部分已修复（`with_session_store` 现按 member 隔离）；范围缩小为仅 log/validator/socket_options/`trusted_proxy_addresses`/TLS 仍来自 primary/find_map 第一个匹配项 | 中 |
| **BUG-68** | `MsgSeqNum too low` 路径跳过 `reset_on_error()` | 需 `ResetOnError=Y` 配置。不一致行为 | 低 |
| **BUG-88** | `forceResendWhenCorruptedStore` 语义分歧 | flag 不做 QFJ 用户期望的事。迁移用户会困惑 | 中 |
| **BUG-90** | AwaitingLogout/Disconnected 状态收到 stray Logon 未拒绝 | 二次核实：004.md 原描述的"发送 Logon 前收到响应"场景在 TrueFix 架构下不会发生（Initiator 的 Logon 在 `on_connected` 中同步发送后才进入 `AwaitingLogon`，故到达该状态时 `logon_sent` 必为 true）；真实存在的是相邻但不同的 gap——`AwaitingLogout`/`Disconnected` 时收到 stray Logon 被 `_ => {}` 静默吞掉 | 低 |
| **BUG-91** | `toAdmin` 设的 ResetSeqNumFlag 不生效 | 应用动态设置 flag 无效。需应用层主动使用此功能 | 中 |
| **BUG-93** | `onLogout` 在握手期间断连不调用 | 应用无信号。需 Logon 握手中途断连 | 低 |
| **BUG-94** | 无 logon 重试退避 | 200ms 轮询轰炸对端。需 Logon 被拒后重试 | 中 |
| **BUG-95** | 负 `HeartBtInt` 静默接受 | 需对端发送负值。心跳不匹配 | 低 |
| **BUG-96** | SequenceReset 后队列未清理 | 内存泄漏。需 gap-fill 跳过多个排队消息 | 低 |
| **BUG-97** | `EndSeqNo=999999` 不被识别为无穷 (FIX ≤4.2，二次核实更正版本号；原文误写 4.1) | 实践中通常正确（999999 > last），但意图未实现 | 极低 |
| **BUG-38** | Data field tag 未验证与预期匹配 | 需畸形消息。错误字段被当作 data 消费 | 中 |
| **BUG-49** | 无效 data-length 静默忽略 | 需非数字 length 字段值 | 中 |
| **BUG-45** | `load_index` 整个 body 文件读入内存 | 大 session open 时 OOM。需长时间运行 + 大量消息 | 中 |
| **BUG-81** | `CachedFileStore` open 时全部消息体物化 | 同上，peak memory = ALL bodies | 中 |
| **BUG-51** | Multi-endpoint rotation 成功后未重置 | 连接 N 断开后从 N+1 而非 primary 开始。运维不便 | 低 |
| **BUG-71** | `tcp_connect` blocking thread leak on timeout | 线程泄漏直到 OS TCP 超时。需 `local_bind_addr` + 超时 | 低 |
| **BUG-70** | MSSQL URL parser 对含 `@` 密码错误 | 需密码含 `@`。配置错误 | 低 |

---

## 🟢 可延后处理 (Can-Defer)

| BUG | 问题 | 延后理由 |
|-----|------|---------|
| **BUG-23**（二次核实降级，原在 🔴） | `frame_length` 整数溢出 | `MAX_BODY_LEN`（16MiB）上限校验已挡在加法之前，溢出路径实际不可达。建议仍补 `checked_add` 作纵深防御（一行），但不再是上线阻塞项 |
| **BUG-36** | heartbeat timeout u32 溢出 | 实际值很小（30×2+2=62），溢出需极端值。`u32` 截断 1.4→1 是真实问题但影响有限 |
| **BUG-47** | `as_int`/`as_decimal` trim 空白 | 宽容解析，实践中不导致正确性问题 |
| **BUG-48** | 时间戳接受非标准小数位数 | 宽容解析。严格对端可能拒绝但 TrueFix 自己的输出是标准的 |
| **BUG-54** | Group required fields 未递归校验 | 需特定字典配置 |
| **BUG-55** | Group field types 未为 group-decoded 消息校验 | 需 `decode_with_groups` 被调用（当前 dormant） |
| **BUG-62** | CHAR 字段验证未版本过滤 | 仅影响 FIX 4.0/4.1，实践中极少使用 |
| **BUG-63** | `precision_key` 大小写敏感 | 配置写大写即可 |
| **BUG-64** | `bool_key` 非"Y"静默为 false | 与 QFJ 行为一致 |
| **BUG-66** | `prepare_resend` 忽略 timestamp_precision | 重发消息固定毫秒精度。实践中可接受 |
| **BUG-67** | Acceptor 截断 peer HeartBtInt via `as u32` | 需对端发送 >u32::MAX 的值 |
| **BUG-69** | AwaitingLogout 不发心跳 | 需长 logout_timeout + 对端不发心跳 |
| **BUG-72** | codegen 遗漏 TIME/PRICEOFFSET 类型 | 仅影响生成的 typed accessor，功能不受影响 |
| **BUG-74** | codegen vs runtime `open` modifier 分歧 | 仅影响使用 `open` modifier 的自定义字典 |
| **BUG-75** | Disconnected 状态仍处理 Logon 序列逻辑 | 需 transport race 条件 |
| **BUG-76** | Acceptor-group TLS 来自不同 member | 需多 session 组 + 非主要 member 有 TLS |
| **BUG-77** | Monitor 未被 Engine::start 连接 | 功能缺失，非正确性问题。可通过 transport 层 API 操作 |
| **BUG-78** | 时间戳拒绝闰秒 (sec=60) | QFJ 接受但闰秒在实际 FIX 流量中极罕见 |
| **BUG-79** | `frame_length` 不验证 BeginString 格式 | 非 FIX 数据会被 framing。需非 FIX 输入 |
| **BUG-80** | decode 接受无 MsgType 的消息 | 需畸形消息。后续处理会失败 |
| **BUG-82** | Monitor status 在断连后过时 | BUG-33 的后果。Monitor 本身未连接 |
| **BUG-83** | GapFill SequenceReset 缺 OrigSendingTime | 严格对端可能要求。QFJ 设置但实践中大多宽容 |
| **BUG-84** | FIX <4.2 未强制秒级时间戳 | 仅影响 FIX 4.0/4.1 |
| **BUG-85** | Acceptor 也有 Logon 超时 | QFJ 仅 initiator 超时。差异但非错误 |
| **BUG-102**（二次核实更正） | `EnableResetSeqTime` 等价功能已实现但未接线 | ❌ 原描述"无等价物"有误——`schedule_reset.rs::decide_recurring_reset` 已完整实现，只是未被 transport/Engine 调用（dormant，与 GAP-26/27 同类）。GAP-11 已追踪为可选 |
| **BUG-103** | resend 非流式 | 内存效率问题。需大量存储消息 |
| **BUG-104** | 无 session enabled 概念 | 功能缺失，非 bug。通过 transport control 实现 |
| **BUG-105** | Reject 前无 logon 状态守卫 | 需在 AwaitingLogon 状态收到非 admin 消息 |
| **BUG-106** | 与 BUG-83 重复 | 已追踪 |
| **BUG-107** | Ticker 未对齐整秒 | 边界检查最多差 999ms。QFGo 独有 |
| **BUG-108** | peer timeout 默认 2 vs QFJ 1.4 vs QFGo 1.2 | TrueFix 更宽松。不是错误，只是差异 |
| **BUG-109** | `shouldSendReset` 只检查 `reset_on_logon` | 需 `reset_on_disconnect=true` + `reset_on_logon=false` |
| **BUG-110** | `closed_resend_interval` 是 no-op | 已文档化。架构理由成立 |
| **BUG-111** | Session 不跨连接持久化 | 架构差异。非 bug，是设计选择 |

---

## ⚪ 非问题 / 描述有误 (Not-a-Bug)

| BUG | 原描述 | 实际情况 |
|-----|--------|---------|
| **BUG-56** | "`admin::base()` 和 `send_app()` 都不设 SubID/LocationID" | `admin::base()` **确实设置了**。仅 `send_app()` 缺失。真实 bug 但范围被夸大 |
| **BUG-65** | `parse_utc_offset` 整数溢出 | 需要 `hours > 596523`，实际 UTC offset 最多 14。理论风险可忽略 |
| **BUG-73** | codegen enum label 是 Rust 关键字时未 sanitize | QFJ enum labels 全是 SCREAMING_SNAKE_CASE，不可能是 Rust 关键字。仅自定义字典理论可能 |
| **BUG-92** | QFJ "每次 fresh start 都发 ResetSeqNumFlag" | QFJ 要求 `(resetOnLogon \|\| resetOnLogout \|\| resetOnDisconnect)` 为 true **且** seqs==1。非无条件 |
| **BUG-24** (release) | Checksum u32 溢出 | Release 构建中 `& 0xFF` 使溢出在数学上无害。仅 debug panic |
| **BUG-108** | "QFGo peer timeout 1.2×" 被当作 bug | 真实差异但不是错误 — TrueFix 默认更宽松，不是更严格 |
| **BUG-36** "2.25× shorter" | 方向性错误 | TrueFix 默认 2×HB+2 比 QFJ 1.4×HB+2 **更长**，不是更短 |
| **BUG-37**（二次核实新增） | `decode_with_groups` 缺 fields_out_of_order | ✅ 已在当前工作树修复——现通过 `section_of` 显式设置该标记（注释标注 GAP-26/FR-032/feature 006） |
| **BUG-98**（二次核实新增） | Logon 在 verify 之前 reset store | ✅ 已在当前工作树修复——CompID/latency/过低 MsgSeqNum 校验现确实排在 `reset_sequences()` 之前 |
| **BUG-101**（二次核实新增） | Parser 不跳过 `8=` 前垃圾字节 | ✅ 已在当前工作树修复——`classify_buffered` 现调用 `next_frame_start` 向前扫描，只丢弃畸形前缀（注释标注 B14/FR-028） |

---

## 生产发布建议

### 第一优先级 — 必须在上线前修复 (15 项，原 18 项，二次核实后 BUG-23/98/101 移出)

这些 bug 会导致以下任一后果：
- **数据丢失**：BUG-30, 31, 26, 41
- **协议破坏**：BUG-28, 29, 34, 89
- **资源泄漏/无法关闭**：BUG-25, 27, 32, 33
- **安全/DoS**：BUG-100
- **调度违规**：BUG-86, 87

大部分是一行到几行的修复。最复杂的是 BUG-28（ResetSeqNumFlag 握手）和 BUG-34（callback 排序），需要重构 `on_logon` 和 `handle_inbound` 的调用顺序。

### 第二优先级 — 建议在第一个生产版本后立即修复 (31 项)

这些是真实 bug 但有 workaround 或需特殊条件触发。可在上线后第一个迭代中修复。

### 第三优先级 — 随常规开发进度修复 (33 项)

低概率、工具链问题、或架构差异。不影响正确性。

### 关于 BUG-24 的特别说明

Checksum `u32` 溢出在 **release** 构建中是无害的——`Iterator::sum::<u32>()` 在 release 模式下使用 wrapping add，而最终的 `& 0xFF` 使得溢出对结果无影响（因为 `(a + b) mod 256` 与 `((a mod 256) + (b mod 256)) mod 256` 在数学上等价，无论中间是否溢出）。**仅 debug 构建会 panic**。

如果生产环境使用 release 构建编译，BUG-24 不阻塞上线。但建议修复以满足 "No path panics" 的 crate 级别 invariant。

### 关于 BUG-111 的特别说明

TrueFix 的 "每连接创建新 Session" 架构是**有意的设计选择**，不是 bug。它简化了状态管理（无需锁、无需跨线程同步），代价是失去了 QFJ 的 `Responder` swap 机制带来的能力（重复连接检测、`validLogonState` 等）。

这些缺失的能力已被单独追踪为 BUG-32（重复连接）、BUG-42（`validLogonState`）、BUG-33（`onLogout` on TCP drop）。BUG-111 本身不需要单独修复——它是其他 bug 的架构根因，不是独立的可操作项。

### 推荐的最小上线前修复清单

```
BUG-26  — 调整 ensure_schema 迁移顺序 (2 行)
BUG-28  — ResetSeqNumFlag echo + verify + infer (中等)
BUG-29  — ResendRequest 跳过 too-high 检查 (中等)
BUG-30  — SeqFile seek+write 替代 truncate (低)
BUG-31  — load_seqnums 返回 Err 而非 unwrap_or(1) (低)
BUG-32  — route_and_run 检查已活跃连接 (中等)
BUG-33  — Event::Disconnected + enter_disconnected (中等)
BUG-34  — 调整 handle_inbound 调用顺序 (中等)
BUG-86  — state.rs 增加 schedule 检查 (中等)
BUG-87  — on_logon 增加 schedule 检查 (低)
BUG-88  — build_resend 支持 forceResendWhenCorruptedStore (低)
BUG-89  — validate_app 不跳过 admin (低)
BUG-25  — run_scheduled_initiator await JoinHandle (低)
BUG-27  — Engine::shutdown 处理 initiators + Drop (中等)
BUG-100 — frame_length 拒绝 body_len==0 (1 行)
```

> 二次核实后移出清单：BUG-23（checked_add，已被 MAX_BODY_LEN 间接修复，降级为可选加固）、
> BUG-98（reset/verify 顺序，已在当前工作树修复）、BUG-101（classify_buffered 扫描跳过垃圾，
> 已在当前工作树修复）。

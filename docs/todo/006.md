# TrueFix Bug Review — Items NOT Tracked in `docs/todo/005.md`

> **Historical audit snapshot.** Later remediation may have resolved or narrowed these findings.
> Preserve the per-item evidence, but use current code/tests and the
> [documentation index](../README.md) for present status.

> Generated 2026-07-05 from a full-codebase re-audit of all 9 crates (`truefix-core`,
> `truefix-dict`, `truefix-session`, `truefix-store`, `truefix-log`, `truefix-transport`,
> `truefix-config`, `truefix`, `truefix-at`), verified directly against current source.
>
> Every item below was cross-referenced against `003.md` (BUG-05–BUG-22, GAP-48–GAP-56,
> B1–B30), `004.md` (BUG-23–BUG-111), and `005.md` (NEW-01–NEW-96, including the missing-features
> table) and confirmed **not** covered by any of them. Items that overlap with existing tracking
> are excluded.
>
> Citations: **TF** = this repo, `crates/`; **QFJ** = `thrdpty/quickfixj/quickfixj-core/
> src/main/java/quickfix/`; **QFGo** = `thrdpty/quickfix/*.go`.
>
> Numbering: `NEW-97` onwards (continuing from `005.md`). `NEW-110`–`NEW-147` came from a
> 2026-07-06 parallel review of all 9 crates; `NEW-148`–`NEW-155` came from a later focused audit
> of the core/session/transport/engine/store critical paths. IDs are intentionally not renumbered:
> `NEW-109` and `NEW-114` were removed after their fixes were verified; `NEW-110`, `NEW-131`,
> `NEW-141`, and `NEW-144` were removed after re-verification showed that they were false
> positives. Keeping the gaps preserves existing external references.

---

## Classification — not everything below is a "bug"

This document follows the same classification scheme as `005.md`. Each item is tagged:

| Category | Count | Description |
|----------|-------|-------------|
| **Bug** | 6+17+7 | A real production-code correctness/data-loss/security defect |
| **Feature-Gap** | 2+12+1 | QFJ/QFGo has a capability TrueFix doesn't implement at all |
| **Default-Divergence** | 3+2+0 | TrueFix's default config value differs from QFJ/QFGo's |
| **Perf/Hygiene** | 1+2+0 | Correctness unaffected — allocation/complexity observation |
| **Security** | 0+0+0 | Security-hardening gap |

Each finding is counted under its displayed primary category. The three terms are the original
review (`NEW-97`–`NEW-108`), the parallel review (`NEW-111`–`NEW-147`, with retired IDs omitted),
and the focused audit (`NEW-148`–`NEW-155`), respectively.

---

## P1 — High (protocol correctness / data integrity)

### NEW-97: `on_start_logout()` silently drops Logout request when session is not `LoggedOn`

**Category:** Bug

**TF**: `crates/truefix-session/src/state.rs` `on_start_logout()` (~L1521-1528).

```rust
fn on_start_logout(&mut self) -> Vec<Action> {
    if self.state == SessionState::LoggedOn {
        // ... send Logout, transition to AwaitingLogout
        vec![self.send_stored(lo)]
    } else {
        Vec::new()  // ← silently dropped
    }
}
```

`on_start_logout` (the handler for `Event::StartLogout`, called when the application requests a
graceful logout) only sends a `Logout` message when `state == LoggedOn`. In any other state
(`AwaitingLogon`, `AwaitingLogout`, `Disconnected`), it returns `Vec::new()` — the application's
logout directive is silently discarded with no action and no notification.

The most impactful case is `AwaitingLogon` for an **initiator**: if the application calls
`logout()` immediately after `Engine::start` (before the counterparty's Logon has arrived), the
Logout is never sent. The initiator remains in `AwaitingLogon` until `logon_timeout` expires,
then is forcibly disconnected — the counterparty never receives a Logout.

**QFJ**: `Session.java`'s `logout()` (line ~940) unconditionally calls `generateLogout()` and
transitions state, regardless of the current session state. Even in `LOGON_RECEIVED` state, QFJ
queues the Logout to be sent once the Logon round-trip completes.

**Impact**: An initiator that decides to cancel a connection during the Logon handshake cannot do
so gracefully — the counterparty sees an abrupt TCP close (after `logon_timeout`) instead of a
proper Logout. For a multi-session engine where the application may shut down individual sessions
dynamically, this is a real operational issue.

**Fix direction**: Either send a Logout in `AwaitingLogon` state (it will arrive after the
counterparty's Logon, which is acceptable), or queue one to be sent once `LoggedOn` is reached, or
at minimum immediately transition to `Disconnected` + `Action::Disconnect` rather than no-op'ing.

---

### NEW-98: Dictionary-validation rejection of an out-of-order Logon does not advance `next_in_seq`

**Category:** Bug

**TF**: `crates/truefix-session/src/state.rs` `on_logon()` dictionary-validation branch (~L1344-1366).

```rust
if let Some(reject) = self.validate_app(&msg) {
    let mut actions = vec![reject];
    if logon_seq_early.is_some_and(|s| s == self.next_in_seq) {
        self.next_in_seq = self.next_in_seq.saturating_add(1);
    }
    // ... disconnect_on_error path ...
    return actions;
}
```

When a Logon fails dictionary validation and its `MsgSeqNum` equals `next_in_seq`, the sequence is
advanced. But when the Logon's `MsgSeqNum` is **greater** than `next_in_seq` (out-of-order), the
`if s == self.next_in_seq` condition fails and `next_in_seq` is **not** advanced.

The Logon is correctly rejected (Reject + optional Logout + Disconnect), but the counterparty's
outbound sequence has advanced — they sent `MsgSeqNum=N` and will next send `N+1`. TrueFix still
expects `next_in_seq` (the old value), so the next connection's first message at `N+1` looks too
high, triggering a `ResendRequest` for a gap that doesn't exist.

**QFJ**: `nextLogon()` (`Session.java` ~L1310) always advances the target sequence on a received
Logon, even when rejecting it — the counterparty's sequence number was consumed.

**Impact**: After rejecting an out-of-order Logon, the next reconnect's first message is
unexpectedly too-high, causing a spurious `ResendRequest` and potential confusion. The desync
self-heals once the resend completes, but it's unnecessary traffic and a real protocol-correctness
gap.

**Fix direction**: In the dictionary-validation rejection branch, advance `next_in_seq` whenever the
Logon's sequence is `>= self.next_in_seq` (not just `==`), or simply advance unconditionally for
any Logon that arrives with a valid `MsgSeqNum`.

---

### NEW-99: `SequenceReset(35=4)` bypasses `validate_app()` dictionary validation entirely

**Category:** Bug

**TF**: `crates/truefix-session/src/state.rs` `on_received()` (~L748).

```rust
if mt.as_deref() == Some("A") {
    return self.on_logon(msg);
}
if mt.as_deref() == Some("4") {
    return self.on_sequence_reset(&msg);  // ← routed before validate_app
}
// ... later: validate_app() runs for everything else
```

`on_received` routes `MsgType=4` (SequenceReset) directly to `on_sequence_reset()` before the
normal `seq.cmp(&self.next_in_seq)` dispatch — which is where `validate_app()` is called (inside
the `Ordering::Equal` branch at ~L788). Every other message type (application messages, and now
even admin messages per BUG-89/FR-011) goes through dictionary validation, but `SequenceReset`
completely skips it.

A malformed `SequenceReset` with illegal field values (e.g. a non-integer `NewSeqNo`, an unknown
tag, a missing required field in a FIXT 1.1 transport dictionary) is accepted and processed
without any dictionary-level check.

**QFJ**: `Session.java`'s `nextSequenceReset()` (line ~1539) calls `verify(sequenceReset, ...)`
which runs the full dictionary validation path (`DataDictionary.validate()`) before processing the
message — `SequenceReset` is not exempt from field-level validation in QFJ.

**Impact**: A counterparty sending a `SequenceReset` with a non-numeric or malformed `NewSeqNo`
tag has it silently accepted by the `.filter(|&n| n > 0)` / `as_int()` chain — which maps it to
`None` and then silently skips the reset — instead of being rejected as a format error. More
broadly, any dictionary-level constraint (required fields, enum values, field types) is bypassed
for this one message type.

**Distinction from NEW-84**: NEW-84 (in `005.md`) is about `SequenceReset` bypassing the
**too-high sequence-number check** (`checkTooHigh`). This item is about bypassing **dictionary
validation** (`validate_app`) — a completely different check that runs at a different layer. Both
gaps exist independently.

**Fix direction**: Move the `MsgType=4` routing to after the `validate_app()` call in the
`Ordering::Equal` branch, or add an explicit `validate_app()` call inside `on_sequence_reset()`
before processing the message.

---

### NEW-100: `send_stored` persists teardown Logout messages — stale Logout can be resent on next connection

**Category:** Bug

**TF**: `crates/truefix-session/src/state.rs` `send_stored()` (~L508-523), called from
`on_received()`'s latency-failure path (~L708) and identity-failure path (~L719).

When a non-Logon message fails the latency check or CompID check, `on_received` sends a `Logout`
message via `send_stored(admin::logout(...))`. `send_stored` — its name notwithstanding — stores the
message into `self.store` (keyed by `MsgSeqNum`) whenever `config.persist_messages` is true:

```rust
fn send_stored(&mut self, mut msg: Message) -> Action {
    // ...
    if self.config.persist_messages
        && let Some(seq) = msg.header.get(MSG_SEQ_NUM) ... {
        self.store.insert(seq as u64, msg.clone());  // ← Logout stored
    }
    Action::Send(msg)
}
```

If `ResetOnDisconnect` and `ResetOnLogout` are both `false`, the store is not cleared on teardown.
On the next connection, if the counterparty sends a `ResendRequest` covering this sequence number,
the stored Logout is resent as part of gap-fill recovery — the counterparty receives a spurious,
out-of-context `Logout(35=5)` message that was originally sent to signal a latency/identity failure
on a prior connection.

This is distinct from any Logout sent during the normal Logout handshake (`on_logout_msg` /
`on_start_logout`), which is also stored but is at least contextually correct to resend (it represents
a legitimate part of the session history). The latency/identity teardown Logout is a one-shot
diagnostic message that should never be replayed.

**QFJ**: QFJ's `Session.java` does not store teardown/error messages in the message store —
`disconnect()` / `generateLogout()` are sent via `sendRaw()` which bypasses storage, while
`send()` (which stores) is used only for normal sequenced traffic.

**Impact**: A counterparty performing gap recovery after a reconnect may receive an unexpected
Logout, potentially triggering their own logout/disconnect logic and tearing down the freshly
re-established session.

**Fix direction**: Use `send_raw()` (which does not store) instead of `send_stored()` for
teardown-error Logout messages, matching QFJ's `sendRaw()` usage for the same purpose. Alternatively,
remove Logout messages from the store during `enter_disconnected()` when the teardown was
error-driven rather than a normal Logout handshake.

---

### NEW-101: `as_int`/`as_decimal` use `.trim()` — silently accept whitespace-padded wire values

**Category:** Bug

**TF**: `crates/truefix-core/src/field.rs` `as_int()` (~L120), `as_decimal()` (~L128).

```rust
pub fn as_int(&self) -> Result<i64, ...> {
    let s = self.as_str()?;
    s.trim().parse::<i64>().map_err(...)
}

pub fn as_decimal(&self) -> Result<Decimal, ...> {
    let s = self.as_str()?;
    s.trim().parse::<Decimal>().map_err(...)
}
```

Both `as_int()` and `as_decimal()` call `.trim()` on the string value before parsing. A wire value
like `"  123  "` (with leading/trailing spaces or other whitespace) is silently accepted and parsed
as `123`, even though the FIX specification requires integer and decimal field values to be
strictly numeric with no surrounding whitespace.

The raw bytes are preserved for round-trip fidelity (the field stores the original value), but the
typed accessor returns a false-positive `Ok(123)` for what is actually a malformed wire value. This
infects `value_ok()` in the dictionary validator, which calls `as_int().is_ok()` — a
whitespace-padded integer passes type validation when it should be rejected.

**QFJ**: QFJ's `IntConverter` (`IntConverter.java`) and `DecimalConverter` call
`Long.parseLong(s)` / `new BigDecimal(s)` directly, with **no trimming** — surrounding whitespace
causes a `NumberFormatException`. The strict behavior is intentional: wire values should not
contain extraneous whitespace, and accepting them silently can mask encoding bugs.

**Impact**: A counterparty sending `"  123  "` as an integer field value passes TrueFix's type
validation but would be rejected by QFJ's strict converter — interoperability divergence. The
whitespace also round-trips to the wire on re-encode, propagating the malformed value.

**Fix direction**: Remove `.trim()` from `as_int()` and `as_decimal()`, relying on `.parse()` to
reject whitespace-padded values. If there's a concern about real-world tolerance, make strict
parsing the default and add a separate lenient accessor if needed.

---

**Correction to `005.md`'s own record — `NEW-54` is already fixed, not open.** `005.md` (§NEW-54)
describes `MongoStore::set_seq` writing to a **literal** key `"field"` via
`doc! { "$set": { field: seq as i64 } }`. Reading `bson` 2.15's `doc!` macro expansion
(`bson-2.15.0/src/macros.rs`), a bare (non-parenthesized, non-string-literal) identifier key is
*not* stringified — it's spliced into `object.insert($key, $value)` as an expression, i.e. it
evaluates the Rust variable `field` (bound two lines above to `"sender"` or `"target"`) as the
key's *value*. Current `crates/truefix-store/src/mongo.rs:139-148` is therefore already correct.
Whether this was fixed before `005.md`'s own pass or `005.md` mis-attributed it is unclear — either
way, no code change is needed here; noted so a future pass doesn't re-open it.

---

## P2 — Medium (default divergence / feature gap)

### NEW-102: `heartbeat_timeout_multiplier` default 2.0 diverges from QFJ's 1.4

**Category:** Default-Divergence

**TF**: `crates/truefix-session/src/config.rs:201` — `heartbeat_timeout_multiplier: 2.0`.

The heartbeat timeout is computed as `HeartBtInt × multiplier + 2` ticks. With the default
`HeartBtInt=30` and `multiplier=2.0`, TrueFix waits 62 seconds before disconnecting a silent peer.

**QFJ**: `DefaultSessionFactory.java` defaults `HeartBtIntTimeoutMultiplier` to `1.4`
(`Session.java` ~L210: `DEFAULT_HEARTBT_TIMEOUT_MULTIPLIER = 1.4`). With the same HeartBtInt=30,
QFJ waits 44 seconds.

**Impact**: TrueFix detects a dead peer ~40% slower than QFJ at default settings. Not a correctness
issue (an operator who sets the value explicitly sees no divergence), but a migration surprise.

**Fix direction**: Change the default from `2.0` to `1.4`, matching QFJ.

---

### NEW-103: `test_request_delay_multiplier` default 1.0 diverges from QFJ's ~0.5

**Category:** Default-Divergence

**TF**: `crates/truefix-session/src/config.rs:201` — `test_request_delay_multiplier: 1.0`.

The TestRequest probe fires after `HeartBtInt × multiplier` idle ticks. With the default
`HeartBtInt=30` and `multiplier=1.0`, TrueFix sends a TestRequest after 30 seconds of silence.

**QFJ**: QFJ sends a TestRequest after approximately `HeartBtInt / 2` seconds of silence
(`Session.java` ~L2035: the test-request counter threshold is `heartbeatInterval / 2`, rounded).
With HeartBtInt=30, QFJ probes after ~15 seconds.

**Impact**: TrueFix takes twice as long as QFJ to probe a silent peer at default settings, delaying
detection of a half-open connection.

**Fix direction**: Change the default from `1.0` to `0.5`, matching QFJ's behavior.

---

### NEW-104: `ValidateFieldsOutOfOrder` config default `false` diverges from QFJ's `true`

**Category:** Default-Divergence

**TF**: `crates/truefix-config/src/builder.rs:625` —
`validate_fields_out_of_order: bool_key(map, "ValidateFieldsOutOfOrder", false)`.

When a `.cfg` does not set `ValidateFieldsOutOfOrder`, TrueFix defaults to `false` — out-of-order
fields (e.g. a body field appearing in the header, or a trailer field before the body) are silently
accepted. The decoder does set `msg.fields_out_of_order = true` (used for reporting), but
validation does not reject the message.

**QFJ**: `DataDictionary.java` defaults `checkFieldsOutOfOrder` to `true`
(`DataDictionary.java` ~L95: `setCheckFieldsOutOfOrder(true)` in the constructor). An out-of-order
field produces a `SessionRejectReason=14` (Tag specified out of order).

**Impact**: A message with fields in the wrong section (header/body/trailer) passes TrueFix
validation but would be rejected by QFJ — interoperability divergence. This is especially relevant
for fields that the static `is_header`/`is_trailer` classification misroutes (since those checks
also use the same classification).

**Fix direction**: Change the default in `builder.rs` from `false` to `true`, matching QFJ.

---

### NEW-105: No `CancelOrdersOnDisconnect` support

**Category:** Feature-Gap

**TF**: `crates/truefix-session/src/state.rs` — no `cancel_orders_on_disconnect` field or handling.

QFJ's `CancelOrdersOnDisconnect` config key (Session setting) causes the session to automatically
send `OrderCancel` (`35=F`) requests for all open orders when the TCP connection is lost, before
the session disconnects. This is a safety mechanism for trading systems: when connectivity to an
exchange is lost, outstanding orders remain live on the exchange side, and the firm may want to
cancel them automatically rather than risk unmanaged exposure.

TrueFix has no equivalent: on disconnect, the session simply transitions to `Disconnected` and
drops. The `Application` trait's `on_disconnect()` callback is the only notification, but the
application must implement all cancel logic itself, including order tracking, message
construction, and sequencing — with no session-level support for sending messages during teardown.

**QFJ**: `Session.java`'s `disconnect()` (~L1620) checks `cancelOrdersOnDisconnect` and calls
`generateOrderCancelRequests()` before the socket is closed, sending cancels for all tracked open
orders using the session's own sequence-number space.

**Impact**: Trading system operators migrating from QFJ must implement their own cancel-on-disconnect
logic at the application layer, which is error-prone (the session is tearing down, sequencing is
complex, and there may not be a reliable send path during teardown).

**Fix direction**: Add a `cancel_orders_on_disconnect: bool` config field, and in
`enter_disconnected()` (or just before it), if set, emit `Action::Send` for order-cancel messages
for all tracked orders. This requires an order-tracking mechanism, which may need to be an
`Application`-layer callback (`fn open_orders(&self) -> Vec<Order>`) or a session-level store.

---

### NEW-106: No `PossResend(97)` support

**Category:** Feature-Gap

**TF**: `crates/truefix-session/src/state.rs` — no handling of tag 97 (`PossResend`).

FIX protocol defines `PossResend(97)` as a boolean field on application messages indicating "this
message may have been previously sent" — distinct from `PossDupFlag(43)`, which is a session-level
tag indicating the message is a duplicate at the transport layer. `PossResend` is set by the
**application** (not the session) when it re-sends a business-level message that may not have been
confirmed, and the receiving application is expected to perform its own deduplication.

QFJ passes `PossResend` to the `Application::fromApp()` callback so the application can check
whether it has already processed the message and take appropriate action (skip, deduplicate, or
process as new).

TrueFix does not parse, surface, or pass `PossResend` to the application at all. An inbound message
carrying tag 97 is decoded normally; the field lands in `message.body` (or `message.header`,
depending on tag classification) and is never inspected by the session layer. The
`Application::from_app()` callback has no `poss_resend: bool` parameter.

**QFJ**: `Session.java`'s `nextAppMessage()` (~L2480) extracts `PossResend(97)` and passes it to
`Application.fromApp(message, sessionId, possResend)` — the application receives a boolean flag and
can deduplicate accordingly.

**Impact**: An application built on TrueFix cannot distinguish a first-time message from a
possible re-send, preventing correct application-level deduplication. For trading systems where
idempotency is critical (e.g. avoiding double execution of an order), this is a real gap.

**Distinction from 005.md's missing-features table**: The missing-features table lists 6 items
(`EncryptMethod`, `Username`/`Password`, `LastMsgSeqNumProcessed` validation, `MaxMessagesInResendRequest`,
`MessageStore::refresh()`, `FixtDictionaries` validate/dispatch helper). `PossResend` is **not**
among them.

**Fix direction**: Parse `PossResend(97)` from the inbound message in `on_received()` and pass it
to `Application::from_app()` (or `from_admin()`) as an additional parameter, matching QFJ's
signature. Alternatively, leave it in the `Message` and document that the application should check
`msg.body.get(97)` itself — but this is less ergonomic and easy to miss.

---

## P3 — Low (hardening / edge case)

### NEW-107: `frame_length` does not verify SOH byte at the CheckSum trailer's end

**Category:** Bug

**TF**: `crates/truefix-core/src/framing.rs` `frame_length()` (~L74).

BUG-46/FR-047 (feature 007) added a verification that `buf[total-7..total-4] == b"10="` — confirming
the CheckSum tag is present at the expected position. However, the function does **not** verify that
`buf[total-1] == SOH` (the byte immediately after the three-digit checksum). QFJ's
`FIXMessageDecoder` verifies the complete `10=\\d{3}\\x01` trailer pattern.

A malformed frame where `BodyLength` is correct, `10=` appears at the right position, and the
checksum digits are numeric, but the byte after the checksum is not SOH (e.g. it's a letter or
digit), would be accepted as a complete frame by `frame_length`. The subsequent `tokenize_validated`
call would then parse the non-SOH byte as the start of the next field, potentially misframing the
remainder of the stream.

**Impact**: Very narrow — in practice, FIX encoders always emit `10=XXX<SOH>` as the final 7 bytes,
so a real message always has SOH at this position. The gap only matters for adversarial or
deeply-malformed input that happens to have a correct `BodyLength` and `10=` prefix but a wrong
trailing byte.

**QFJ**: `FIXMessageDecoder` (line ~120) checks `header[headerLength-1] == 0x01` as part of its
trailer validation.

**Fix direction**: Add `buf.get(total - 1) == Some(&SOH)` to the existing `10=` check.

---

### NEW-108: No file log or message store rotation / size limits

**Category:** Perf/Hygiene

**TF**: `crates/truefix-log/src/file.rs` `FileLog::open()`;
`crates/truefix-store/src/file.rs` `BodyLog`.

`FileLog` opens the log file in append-only mode and writes indefinitely — there is no rotation,
truncation, or size cap. A long-running session accumulates every log entry in a single file that
grows without bound.

Similarly, `BodyLog` (the file-based message store) appends every sent message body to a single
file with no rotation or compaction. A session that sends millions of messages over its lifetime
accumulates a body file that can grow to many gigabytes, with `read()` (used during resend)
performing `seek` + `read` for each message — O(1) per message, but the file handle and disk
footprint grow unbounded.

**QFJ**: QFJ's `FileLog` and `FileStore` also have no built-in rotation — this is parity, not a
divergence. QFJ deployments typically use external log rotation (e.g. `logrotate`) and rely on
session resets (which truncate the body file) to bound growth. However, QFJ at least documents this
as an operational concern; TrueFix does not.

**Impact**: Disk exhaustion on long-running deployments without external rotation. Not a correctness
issue — operational hygiene only.

**Fix direction**: Document the need for external log rotation in operational docs. Optionally, add
a `MaxFileLogSize` config key that triggers rotation when the log/body file exceeds a threshold,
matching QFJ's `FileLogMaxSize` (though QFJ itself does not implement this — it's a common
deployment-level concern).

---

## 2026-07-06 Parallel Review — All 9 Crates (`NEW-110`–`NEW-147`)

> Each crate was reviewed independently by a dedicated agent that read all source files, compared
> against QFJ/QFGo, and cross-referenced every finding against `003.md`–`006.md` (including the
> items above). Duplicates with `005.md` or `006.md` were excluded. The `truefix-at` crate review
> timed out before completing; its findings are not included here (AT-harness issues are already
> extensively tracked in `005.md` NEW-15/16/28/29/30/50/51/52/83).
>
### Classification of parallel-review items

| Category | Count | Description |
|----------|-------|-------------|
| **Bug** | 17 | Real correctness/data-loss/security defect |
| **Feature-Gap** | 12 | QFJ/QFGo capability TrueFix doesn't implement |
| **Default-Divergence** | 2 | Default config value differs from QFJ |
| **Perf/Hygiene** | 2 | Correctness unaffected — allocation/complexity |
| **Security** | 0 | Security-hardening gap (build-time only) |

---

### NEW-111: `AllowPosDup` default `true` diverges from QFJ's `false`

**Category:** Default-Divergence

**Severity:** P2

**TF**: `crates/truefix-config/src/builder.rs:644` —
`allow_pos_dup: bool_key(map, "AllowPosDup", true)`.

TrueFix allows PossDup messages without `OrigSendingTime` by default. QFJ's `Session.java`
constructor sets `allowPossDup = false`; `SETTING_ALLOW_POS_DUP_MESSAGES` defaults to `N`.

**Impact**: Silently weakens anti-replay protection. A PossDup message missing `OrigSendingTime`
passes TrueFix validation but would be rejected by QFJ.

**Fix direction**: Change default from `true` to `false`, matching QFJ.

---

### NEW-112: `EnabledProtocols` silently falls back to Tls12 for unrecognized values

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-config/src/builder.rs:904-907` — `resolve_tls()`:

```rust
let min_version = match map.get("EnabledProtocols").map(String::as_str) {
    None => None,
    Some(v) if v.contains("1.3") && !v.contains("1.2") => Some(TlsVersion::Tls13),
    Some(_) => Some(TlsVersion::Tls12),  // ← catch-all, never errors
};
```

Any unrecognized value (typo, `SSL3.0`, `TLS1.4`, garbled text) silently defaults to Tls12 with no
diagnostic.

**Impact**: Misconfigured TLS protocol settings go undetected, potentially running with a
weaker/unexpected protocol version.

**Fix direction**: Reject unrecognized values with `ConfigError::InvalidValue` instead of silently
falling through to `Tls12`.

---

### NEW-113: `bool_key` silently treats non-Y/N values as `false` for all boolean keys except `SocketUseSSL`

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-config/src/builder.rs:320-327`:

```rust
fn bool_key(map: &Map, key: &str, default: bool) -> bool {
    map.get(key)
        .map(|v| v.eq_ignore_ascii_case("Y"))
        .unwrap_or(default)
}
```

Only `SocketUseSSL` (via `strict_bool_key` at line 345) treats non-Y/N values as errors. All other
boolean keys silently return `false` for any value that isn't `Y`/`y` — including `TRUE`, `yes`,
`1`, or typos like `ResetOnLogon=YES`.

**QFJ**: `BooleanConverter.convert()` throws `FieldConvertError` on non-Y/N values.

**Impact**: Typos like `CheckLatency=true` or `DisconnectOnError=yes` silently disable features
instead of surfacing a config error.

**Fix direction**: Convert all boolean keys to use `strict_bool_key`, or make `bool_key` strict by
default.

---

### NEW-115: Accept loop silently exits on any transient error

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-transport/src/lib.rs` `Acceptor::serve()` (~L500) and
`AcceptorBuilder::serve()` (~L1400).

Both accept loops use `while let Ok((mut stream, peer)) = ...accept().await`, which silently exits
on ANY accept error — including transient OS errors like `EMFILE` (too many open files),
`ECONNABORTED`, `ENFILE`, or temporary resource exhaustion.

**QFJ**: Mina's accept loop catches `IOException` and continues — only stops on intentional
shutdown.

**Impact**: A single transient OS error permanently kills the acceptor with no logging. All
subsequent connection attempts are silently ignored until process restart.

**Fix direction**: Log the error and continue on transient errors; only break on fatal errors
(e.g. listener closed).

---

### NEW-116: MongoStore/SqlStore/MssqlStore `reset()` not transactional

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-store/src/mongo.rs:182-194`, `sql.rs:278-330`, `mssql.rs:405-422`.

All three use separate DELETE (messages) then UPDATE (seq nums) calls. A crash between them leaves
messages deleted but seq nums at pre-reset values. Contrast with `RedbStore::reset()` which
correctly wraps everything in a single write transaction.

**Impact**: Crash-during-reset leaves the store in an inconsistent state: sequence numbers indicate
pre-reset values but all stored messages are gone. Resend requests will return gap-fills for all
sequences.

**Fix direction**: Wrap both operations in a single transaction (MongoDB 4.0+ supports
transactions; SQL backends already have transaction support).

---

### NEW-117: `save_and_advance_sender` — body failure after seq advance in FileStore

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-store/src/file.rs:355-362, 474-485`.

`seq.set_sender(seq+1)` succeeds, then `body.append()` fails (disk full, I/O error). Seq is
persisted with no message body. Caller gets an `Err` but the seq is already advanced. On restart,
resend for `seq` hits gap-fill (`SequenceReset`) instead of the original message.

**Distinction from NEW-08** (in `005.md`): NEW-08 is about MongoStore lacking a
`save_and_advance_sender` override entirely. This is about FileStore's override having a different
crash window — seq is advanced first, then body write can fail.

**Impact**: Permanent message loss for the affected sequence number after a disk-full/I/O-error
during send.

**Fix direction**: Write the body first, then advance the sequence number; or use a single atomic
operation.

---

### NEW-118: NoopStore diverges from QFJ — does not track sequence numbers in memory

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-store/src/noop.rs:1-30`.

TrueFix's NoopStore always returns 1 for `next_sender_seq()`/`next_target_seq()`, discarding all
`set_*` calls. QFJ's NoopStore tracks sequence numbers in memory during the session lifetime
(`setNextSenderMsgSeqNum(N)` → `getNextSenderMsgSeqNum()` returns `N`).

**Impact**: Any code path using NoopStore (e.g. test harness, lightweight sessions) has broken
sequence-number tracking — the session always thinks it's at seq 1, causing the peer to reject
every message after the first.

**Fix direction**: Store sequence numbers in memory (two `AtomicU64` fields) and return them from
`next_sender_seq()`/`next_target_seq()`.

---

### NEW-119: Non-gap-fill `SequenceReset` leaks stale out-of-order queue entries

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-session/src/state.rs` `on_sequence_reset()` (~L1030-1042).

When a plain-mode (non-gap-fill) `SequenceReset(35=4)` arrives, `on_sequence_reset` sets
`self.next_in_seq = ns` and calls `self.drain_queue()`. But it does NOT call
`apply_gap_fill_new_seq()` — which for the gap-fill path does
`self.queue.retain(|&seq, _| seq >= ns)`. Any out-of-order messages queued at sequences below the
new `ns` are never cleaned out. `drain_queue`'s `while let Some(msg) = self.queue.remove(&self.next_in_seq)`
starts at the new `ns`, so those lower-sequence entries sit in the queue permanently.

```rust
// Non-gap-fill path:
if let Some(ns) = new_seq {
    self.next_in_seq = ns;       // jump forward
}
return self.drain_queue();       // queue NOT cleaned below ns!

// Compare gap-fill path:
self.apply_gap_fill_new_seq(new_seq);  // clears stale queue entries
self.drain_queue()
```

**Impact**: Permanent memory leak. A legitimate plain SequenceReset (e.g. session reset by
administrator) permanently retains stale OOO messages, growing the queue indefinitely across
resets.

**Fix direction**: Call `self.queue.retain(|&seq, _| seq >= ns)` (or
`self.apply_gap_fill_new_seq(Some(ns))`) before `drain_queue()` in the non-gap-fill branch.

---

### NEW-120: `drain_queue` disconnects on validation failure without sending Logout

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-session/src/state.rs` `drain_queue()` (~L981-991).

When a message drained from the out-of-order queue fails `validate_app()` with
`disconnect_on_error=true`, the code disconnects immediately — but does NOT send a Logout. The
in-order path (`on_received`, `Ordering::Equal` arm), which was fixed by B5/FR-008, sends a Logout
before disconnecting.

```rust
// drain_queue path — NO LOGOUT:
if self.config.disconnect_on_error {
    self.state = SessionState::Disconnected;
    actions.push(Action::Disconnect);  // ← bare disconnect

// Compare in-order path (B5 fix):
if self.config.disconnect_on_error {
    let lo = admin::logout(&self.config, seq, Some("..."));
    actions.push(self.send_stored(lo));   // ← LOGOUT SENT
    self.state = SessionState::Disconnected;
    actions.push(Action::Disconnect);
}
```

**Impact**: Protocol violation — peer sees ungraceful disconnect for a recoverable validation error
during gap recovery, instead of a proper Logout.

**Fix direction**: Mirror the B5/FR-008 fix in `drain_queue`: send a Logout before disconnecting on
validation failure.

---

### NEW-121: `SessionHandle`/`ReconnectHandle`/`ScheduledHandle` expose no `SessionId`

**Category:** Feature-Gap

**Severity:** P2

**TF**: `crates/truefix-transport/src/lib.rs` — `SessionHandle` (~L246), `ReconnectHandle`
(~L2100), `ScheduledHandle` (~L2380); `crates/truefix/src/lib.rs` — `Engine::initiators()` (~L420).

None of the three handle types carry a `SessionId` field. `Engine::initiators()` returns
`&[SessionHandle]` with no way to correlate a handle to a session. A caller with 10 initiator
sessions has 10 handles in an opaque slice — the only way to match handle-to-session is to rely on
config-order stability (undocumented, fragile).

**QFJ**: `Session` object IS the handle; `SessionID` is directly queryable via
`session.getSessionID()`. QFGo's `Session` embeds `SessionID` as a field.

**Impact**: Multi-session management (logout a specific session, send on a specific session, monitor
health per-session) requires fragile out-of-band tracking.

**Fix direction**: Add a `session_id: SessionId` field to each handle type and a
`pub fn session_id(&self) -> &SessionId` accessor.

---

### NEW-122: Acceptor sessions have no per-session handle from `Engine`

**Category:** Feature-Gap

**Severity:** P2

**TF**: `crates/truefix/src/lib.rs` — `Engine` struct (~L218), `Engine::start` single-acceptor path
(~L345-370), grouped-acceptor path (~L272-340).

Initiator sessions get handles returned via `initiators()`/`failover_initiators()`. Acceptor sessions
store only `Vec<JoinHandle<()>>` — a bare task handle with no control channel and no session
identity. There is no way to gracefully logout a single acceptor session, send an application message
on an acceptor-originated session, query whether a specific acceptor session is connected, or reset a
specific acceptor session. The only operation is `shutdown()` which abort-kills everything.

**QFJ**: `SocketAcceptor` exposes `getSessions()` returning `HashMap<SessionID, Session>` — every
session is individually addressable.

**Impact**: An operator can't programmatically manage acceptor sessions through the `Engine` API at
runtime.

**Fix direction**: Return a `SessionHandle`-like struct for single-session acceptors; for grouped
acceptors, provide a session-registry mapping `SessionId → mpsc::Sender<Control>`.

---

### NEW-123: `Engine` has no session state query API / Monitor integration

**Category:** Feature-Gap

**Severity:** P2

**TF**: `crates/truefix/src/lib.rs` — `Engine` struct (~L218).

The `Engine` API surface is only `start()`, `initiators()`, `failover_initiators()`, and
`shutdown()`. Missing: `session_ids()`, `session_state(id)`, `is_connected(id)`, or any integration
with `Monitor` (the operational monitoring surface exists in `Services` but `Engine::start` never
wires it — `..Services::default()` hardcodes `monitor: None`).

**QFJ**: Both `SocketAcceptor`/`SocketInitiator` expose session lookup by ID and state query. QFGo's
`Acceptor`/`Initiator` have `GetSession(sessionID)` returning the live session.

**Impact**: Operational tooling (health checks, dashboards, automated monitoring) must build its own
session tracking outside the `Engine`.

**Fix direction**: Add an optional `Monitor` to `Engine` (created by `Engine::start` by default);
expose `fn monitor(&self) -> Option<&Monitor>` and convenience query methods.

---

### NEW-124: FileLog silently swallows I/O write errors

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-log/src/file.rs:108-112` — `write_line` uses `let _ = writeln!(f, "{line}");`
with no fallback.

**QFJ**: QFJ's `FileLog` catches `IOException` and prints to `System.err.println(...)`.

**Impact**: Disk-full, permission-revoked, and similar failures are completely invisible. Log entries
are silently lost with no diagnostic.

**Fix direction**: At minimum, print to stderr on write failure (matching QFJ); optionally surface
via a health/monitoring callback.

---

### NEW-125: FileLog events not force-timestamped

**Category:** Default-Divergence

**Severity:** P2

**TF**: `crates/truefix-log/src/file.rs:129-136` — `on_event` uses `self.timestamp_prefix()` which
returns empty string when `include_timestamp=false`.

**QFJ**: `FileLog.onEvent()` always passes `forceTimestamp=true`, unconditionally timestamping events
regardless of `FileIncludeTimeStampForMessages`.

**Impact**: At defaults, TrueFix log events have no timestamps while QFJ events do, making it harder
to correlate events across log files and sessions.

**Fix direction**: Always timestamp event lines regardless of `include_timestamp` (which should only
govern message logging, not event logging).

---

### NEW-126: `LogConfig` carries no backend options

**Category:** Feature-Gap

**Severity:** P2

**TF**: `crates/truefix-log/src/lib.rs:100-105` — `LogConfig::File { dir }` carries only a directory
(no `FileLogOptions`), and `Screen`/`Tracing` variants carry nothing. Backends are always constructed
with hardcoded defaults. The direct file-log path (`LogSpec` → `build_log`) correctly threads options;
only the `LogConfig`-based paths are affected.

**Impact**: Operators using `LogConfig`-based construction cannot configure log options like
`include_timestamp`, `include_session_id`, etc.

**Fix direction**: Extend `LogConfig::File` to carry `FileLogOptions` (or a sub-struct), and thread
it through to the backend constructor.

---

### NEW-127: Header/Trailer required-field checking gap vs QFJ

**Category:** Feature-Gap

**Severity:** P2

**TF**: `crates/truefix-dict/src/validate.rs:209-217`, `model.rs:296-300`.

QFJ's `DataDictionary.checkHasRequired()` checks required fields for `HEADER_ID`, `TRAILER_ID`,
AND the message body. TrueFix's `validate()` only checks `mdef.required` (body fields). The
`DataDictionary` model stores `header`/`trailer` as flat `BTreeSet<u32>` — there is no
required/optional split for header/trailer fields, so required-header-field validation is
structurally impossible.

In practice, header/trailer required fields (BeginString(8), BodyLength(9), MsgType(35),
SenderCompID(49), TargetCompID(56), MsgSeqNum(34), SendingTime(52)) are always present by
construction (the codec layer fills them in), making this gap unlikely to surface.

**Fix direction**: Either add `header_required`/`trailer_required` to `DataDictionary`, or document
that header/trailer required-field checking is intentionally not performed (fields are guaranteed
present by construction).

---

### NEW-128: Initiator port-key error label is `"SocketConnectHost/Port"` not `"SocketConnectPort"`

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-config/src/builder.rs:1061-1063` — `resolve_address()` for initiator:

```rust
ConnectionType::Initiator => (
    required(map, "SocketConnectHost", session)?,
    "SocketConnectHost/Port",  // ← confusing error key
),
```

When `SocketConnectPort` is missing, the error reports `SocketConnectHost/Port not defined`, which
confuses users into thinking `SocketConnectHost` is also missing.

**Fix direction**: Use `"SocketConnectPort"` as the port_key for initiators.

---

### NEW-129: No duplicate `[SESSION]` block detection

**Category:** Feature-Gap

**Severity:** P3

**TF**: `crates/truefix-config/src/lib.rs:130-147` — `SessionSettings::parse()` accumulates sessions
into a `Vec` without deduplication.

**QFJ**: `SessionSettings.storeSection()` indexes sessions by `SessionID`, naturally deduplicating.
QFGo explicitly errors on duplicates.

**Impact**: Two `[SESSION]` blocks with identical BeginString/SenderCompID/TargetCompID silently
create two session instances, leading to unpredictable behavior.

**Fix direction**: After parsing, detect duplicate session identities and error with a typed
`ConfigError`.

---

### NEW-130: `#` comment stripping may truncate values containing `#` with preceding space

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-config/src/lib.rs:160-181` — `strip_comment()`.

A value like `Password= ab#cd` strips everything from `#` onward (because `#` is preceded by
whitespace), leaving `Password= ab` instead of `ab#cd`.

**QFJ**: QFJ only treats a `#` at the very start of a line as a comment (via `Tokenizer`), never
mid-value.

**Impact**: Config values containing `#` with a preceding space are silently truncated. The existing
test only covers `ab#cd` (no preceding space).

**Fix direction**: Only strip `#` at the start of a line, or document the behavior more prominently.

---

### NEW-132: `SocketOptions::apply()` silently discards all errors

**Category:** Feature-Gap

**Severity:** P3

**TF**: `crates/truefix-transport/src/lib.rs:105-124`.

Every socket option setter discards errors with `let _ = ...`. If
`set_recv_buffer_size(1048576)` fails because the OS limit is lower, or `set_tos(0x10)` fails on a
platform that doesn't support it, there is zero feedback.

**QFJ**: `NetworkingOptions.apply()` logs each option as it is applied.

**Fix direction**: At minimum, log a warning when an explicitly-configured option fails.

---

### NEW-133: Per-message allocation via `drain().collect()` in `classify_buffered()`

**Category:** Perf/Hygiene

**Severity:** P3

**TF**: `crates/truefix-transport/src/lib.rs:1047`:

```rust
let raw: Vec<u8> = buf.drain(..total).collect();  // alloc per message
```

For a high-throughput session processing thousands of messages per second, this is per-message
allocation pressure in the hottest read path.

**QFJ/QFGo**: QFGo's parser operates on byte slices from the read buffer. QFJ's Mina reads into
pooled buffers. Neither allocates a separate `Vec` per message.

**Fix direction**: Consider replacing `Vec<u8>` with `bytes::BytesMut` and use `split_to()` (O(1),
zero-alloc).

---

### NEW-134: No TLS variant of `run_scheduled_initiator`

**Category:** Feature-Gap

**Severity:** P3

**TF**: `crates/truefix-transport/src/lib.rs:2400-2490`.

`run_scheduled_initiator` exists for plain TCP but there is no TLS or proxy variant. Users who need
scheduled initiator with TLS must either forgo TLS or implement their own schedule-driven loop.

**Fix direction**: Add `run_scheduled_initiator_tls` following the same pattern as
`connect_initiator_reconnecting_multi_tls`.

---

### NEW-135: `bind_listener_with_options` hardcodes backlog to 1024

**Category:** Feature-Gap

**Severity:** P3

**TF**: `crates/truefix-transport/src/lib.rs:127` — `socket.listen(1024)?;`

Cannot tune for high-connection-rate environments without modifying source.

**Fix direction**: Accept an optional backlog parameter in `SocketOptions` defaulting to 1024.

---

### NEW-136: FileStore/CachedFileStore `reset()` crash-safety — `load_index()` replays old body data

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-store/src/file.rs:332-340, 456-468, 497`.

The comments claim pre-reset body data is "harmless, unindexed" after crash, but `load_index()`
(line 497) replays ALL records from the body file on restart, re-indexing old data. After
crash-during-reset: `next_sender_seq()=1` but `get(1,N)` returns stale messages.

**Fix direction**: Truncate the body file atomically with the seqnum file, or have `load_index()`
respect a "reset" marker.

---

### NEW-137: MemoryStore `save()` silently overwrites without duplicate indicator

**Category:** Feature-Gap

**Severity:** P3

**TF**: `crates/truefix-store/src/memory.rs:62-65` — `BTreeMap::insert` overwrites without returning
whether the key existed.

**QFJ**: QFJ's `MemoryStore.set()` returns `false` on overwrite.

**Impact**: No mechanism in the `MessageStore` trait to signal duplicate seq usage.

**Fix direction**: Consider returning a bool from `save()` or adding a separate `contains(seq)`
method.

---

### NEW-138: `as_bool` uses lossy UTF-8 conversion in error path

**Category:** Perf/Hygiene

**Severity:** P3

**TF**: `crates/truefix-core/src/field.rs:137-146` — `as_bool()` error path uses
`String::from_utf8_lossy`, producing replacement characters for invalid UTF-8 in the `NotBool` error
variant.

**Impact**: Diagnostic quality only — error messages may contain `\u{FFFD}` replacement characters
for invalid UTF-8 input.

**Fix direction**: Use `String::from_utf8` and fall back to a hex representation on failure.

---

### NEW-139: Schedule-driven disconnect sends Logout but does not wait for Logout response

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-session/src/state.rs` `on_tick()` (~L1546-1556).

TrueFix sends a Logout and immediately calls `enter_disconnected` + pushes `Action::Disconnect`. QFJ
sets a `LogoutTimeout` timer and waits for the counterparty's Logout response before tearing down the
connection.

```rust
let lo = admin::logout(&self.config, seq, Some("session schedule window closed"));
let send = self.send_stored(lo);
let reset = self.enter_disconnected(DisconnectReason::Other);
let mut actions = vec![send];
actions.extend(reset);
actions.push(Action::Disconnect);  // immediate teardown, no wait
```

**Distinction from NEW-62** (in `005.md`): NEW-62 reported that no Logout was sent at all. The
current code does send a Logout (NEW-62 appears partially fixed), but does not wait for the peer's
Logout response — a residual gap.

**Impact**: Counterparty sees a Logout immediately followed by TCP close — no opportunity to reply
with its own Logout. The outbound Logout may not even arrive before the TCP connection is torn down.

**Fix direction**: Transition to `AwaitingLogout` state, start the `logout_timeout` counter, and let
`on_tick` handle the timeout-driven disconnect.

---

### NEW-140: Gap-fill `SequenceReset` omits `LastMsgSeqNumProcessed(369)` when configured

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-session/src/state.rs` `send_raw()` (~L1268-1271), `gap_fill()` (~L1280).

`send_raw()` (used only by `gap_fill()`) does not set `LastMsgSeqNumProcessed(369)` — unlike
`send_stored()`, which does set it when `enable_last_msg_seq_num_processed` is true. QFJ includes
tag 369 on gap-fill messages.

**Distinction from NEW-31** (in `005.md`, refuted): NEW-31 was about stale 369 on resent application
messages and was refuted (QFJ doesn't refresh 369 on resend either). This is about 369 being
entirely **absent** from gap-fill SequenceReset messages sent via `send_raw`.

**Impact**: When `EnableLastMsgSeqNumProcessed=true`, gap-fill SequenceReset messages omit tag 369,
diverging from QFJ.

**Fix direction**: Add the same `LastMsgSeqNumProcessed` logic from `send_stored` to `send_raw`.

---

### NEW-142: Acceptor transitions to `LoggedOn` before sequence handling completes

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-session/src/state.rs` `on_logon()` acceptor arm (~L990-1010).

`self.state = SessionState::LoggedOn` is set BEFORE the sequence-related dispatch (queue drain,
ResendRequest from too-high Logon seq, NextExpectedMsgSeqNum gap-fill). In QFJ/QFGo, the state
transition happens AFTER all sequence handling is complete.

**Impact**: Theoretical — current code paths don't expose a bug because `drain_queue` and
`build_resend` don't critically depend on `self.state` being `LoggedOn` vs `AwaitingLogon`. But
future changes could introduce a regression if they add state-dependent logic to those functions.

**Fix direction**: Move the state transition to after all sequence handling is complete.

---

### NEW-143: `resolve_lenient` skipped sessions invisible to callers

**Category:** Feature-Gap

**Severity:** P3

**TF**: `crates/truefix/src/lib.rs:236-244`:

```rust
let (resolved, skipped) = settings.resolve_lenient()?;
for (session, err) in &skipped {
    tracing::error!(...);
}
// `skipped` is dropped here — Engine never stores it, caller never sees it
```

The `skipped: Vec<(String, ConfigError)>` is logged to tracing and then discarded. A caller who
wants to programmatically report which sessions failed to start has no access.

**Impact**: Monitoring/alerting systems can't programmatically enumerate skipped sessions without
re-parsing the config themselves.

**Fix direction**: Either add a `skipped_sessions` field to `Engine`, or return the `skipped` list
alongside `Engine` in the result type.

---

### NEW-145: `BeginString` version-match check is always a no-op for bundled dictionaries

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-dict/src/validate.rs:64-75`.

The version-match check consults `self.version_meta`, which is `None` for every bundled dictionary
(no `version-meta` directive exists in any `.fixdict` file). The check is therefore always skipped.
QFJ's check always runs on the `sessionDataDictionary` which always has a version.

**Distinction from NEW-45** (in `005.md`): NEW-45 is about `parse_fix_begin_string` not
distinguishing FIX 5.0/SP1/SP2 (parsing granularity). This is about `version_meta` being `None`
making the entire check a no-op (different root cause).

**Impact**: A message with `BeginString=FIX.4.4` validated against a `FIX.4.3` dictionary would be
accepted in TrueFix but rejected in QFJ. Currently mitigated by the fact that this check is a no-op
for all bundled dictionaries.

**Fix direction**: Use `self.version` (the plain version string) as a fallback when `version_meta`
is `None`.

---

### NEW-146: Section-order checking redundancy between codec and dict layers

**Category:** Feature-Gap

**Severity:** P3

**TF**: `crates/truefix-dict/src/validate.rs:77-82`.

QFJ checks field section ordering at *parse time* in `Message.parseBody()` — header fields appearing
in the body section trigger `TAG_SPECIFIED_OUT_OF_REQUIRED_ORDER` immediately during parsing. TrueFix
delegates this to `opts.validate_fields_out_of_order` in `validate()`, which defaults to `false`
(NEW-104). The codec layer detects out-of-section fields (`Message::fields_out_of_order()`) but
doesn't reject — it just records the fact.

A message like `8=FIX.4.4|9=...|35=D|49=SENDER|56=TARGET|34=1|52=...|49=SENDER|...` — where
`SenderCompID(49)` appears both in the header AND the body — would be accepted by TrueFix (the second
49 passes `self.is_header(49)` check), but would be rejected by QFJ
(`TAG_SPECIFIED_OUT_OF_REQUIRED_ORDER` at parse time).

**Fix direction**: May be handled at the codec layer or the validate layer. Worth noting for parity.

---

### NEW-147: `check_group_field_value` uses parent group's `gdef` for nested group members

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-dict/src/validate.rs:355-375`.

In the flat-walk group validation (`validate_group`), when a nested group's member field is checked,
`gdef` is the *parent* group's definition. The `gdef.child` for the parent group might not contain
the nested group's member fields. The fallback to `self.field(tag)` covers this case, so the check
still works — but it bypasses the group-scoped `child` dictionary entirely for nested group members
in the flat-walk path. This is inconsistent with the structured-walk path
(`validate_structured_group`) which correctly recurses into the nested group's own child.

**Impact**: Low — the fallback to `self.field(tag)` gives the same result since `child` is derived
from `self.fields`. But it's architecturally inconsistent and could diverge if child dictionaries
ever differ from the parent.

**Fix direction**: In the flat walk's `validate_group`, when checking a member that belongs to a
nested group, use the nested group's own `gdef` (fetched from `self.groups`) instead of the
parent's.

---

## 2026-07-06 Focused Critical-Path Audit (`NEW-148`–`NEW-155`)

> Follow-up review of the core/session/transport/engine/store critical paths. These findings were
> checked against `003.md`–`006.md`; adjacent existing items are called out explicitly where
> needed so their distinct failure modes are not conflated.

### NEW-148: Dynamic acceptor sessions share the primary session's persistent store

**Category:** Bug

**Severity:** P1

**TF**: `crates/truefix/src/lib.rs:439-470`,
`crates/truefix-transport/src/lib.rs:1927-1964`.

An acceptor group builds the first member's `primary_store` and installs it as the group-wide
fallback specifically for connections matched through a dynamic template:

```rust
let primary_store = build_store(&primary.store).await?;
let services = Services {
    store: Some(primary_store.clone()),
    // ...
};
```

`route_and_run()` replaces that fallback only when `registry.session_stores` contains the resolved
`SessionId`. That map is populated for statically registered sessions; a dynamically generated
identity has no entry and therefore keeps the shared primary store.

Different dynamic `SessionId`s are allowed to be connected concurrently because the active-session
registry keys them independently. They consequently read and overwrite the same sender/target
sequence numbers and message history.

**Impact**: Cross-session sequence contamination, wrong-message resend, store resets affecting
unrelated counterparties, and data loss. Even a single dynamic session shares state with the
primary static member.

**Distinction from BUG-61/feature-006 per-session services work**: That fix gives each *static*
member its own store/services. It does not create per-identity services for sessions instantiated
from a dynamic template.

**Fix direction**: Add a dynamic `MessageStore`/`Services` factory keyed by the resolved
`SessionId`, or explicitly reject persistent-store configuration for dynamic templates. Never
reuse one mutable store instance across dynamic identities.

---

### NEW-149: `Engine::shutdown()` stops accept loops but leaves accepted session tasks running

**Category:** Bug

**Severity:** P1

**TF**: `crates/truefix-transport/src/lib.rs:617-679, 1790-1824`,
`crates/truefix/src/lib.rs:842-894`.

Both acceptor implementations spawn a detached task for every accepted connection. The
`JoinHandle<()>` returned by `serve()` represents only the outer accept loop; child connection
handles are neither returned nor registered.

`Engine::shutdown()`/`Drop` abort the handles in `self.acceptors`, which therefore closes the
listeners but has no cancellation path to already-accepted `run_connection` tasks.

**Impact**: After shutdown returns, established counterparties can continue exchanging FIX
messages, invoking application callbacks, mutating stores, and holding sockets. The documented
guarantee that every session task is stopped is false, and dropping an `Engine` does not fully
release its runtime resources.

**Distinction from BUG-27**: BUG-27 added cancellation for plain initiators. This finding concerns
the untracked child tasks created by acceptors, not initiator handles or the accept loop itself.

**Fix direction**: Own accepted tasks in a `JoinSet`/task registry with a shared cancellation token.
Engine shutdown must cancel and, for an async shutdown API, await both the listener and every
active accepted connection.

---

### NEW-150: `InChanCapacity` is bypassed by an unbounded `pending_app` staging queue

**Category:** Bug

**Severity:** P1

**TF**: `crates/truefix-transport/src/lib.rs:1043-1066, 1114-1141, 1145-1202`.

When `in_chan_capacity=Some(n)`, the Tokio application channel is bounded, but decoded application
messages are first appended to an unbounded local queue:

```rust
let mut pending_app: VecDeque<Inbound> = VecDeque::new();
// ...
pending_app.push_back(Inbound::Message(msg));
```

If the bounded channel is full, `app_tx.reserve()` is not ready. The reader continues taking the
socket-read branch, decoding more traffic and growing `pending_app` without any limit. Backpressure
therefore does not propagate to TCP as the configuration and acceptance documentation claim.

**Impact**: A slow `from_app` callback or malicious counterparty can grow process memory without
bound even when the operator explicitly configured a small `InChanCapacity`.

**Fix direction**: Bound the complete pre-processing backlog, including staging and partially read
frames. Preserving arbitrary admin-message lookahead while also guaranteeing a strict total memory
bound requires an explicit policy (bounded reorder window, spill-to-disk, or stop-reading once the
window is full); a bounded second-stage channel alone is insufficient.

---

### NEW-151: TLS handshakes are outside every configured timeout

**Category:** Feature-Gap

**Severity:** P1

**TF**: `crates/truefix-transport/src/lib.rs:661-670, 1811-1818, 2310-2329`; the direct and
proxy-TLS initiator functions have the same unbounded `connector.connect(...).await` shape.

`connect_timeout` bounds TCP connection/proxy establishment, and the routed acceptor's
`prelogon_read_timeout` starts only *after* TLS succeeds. No timeout wraps
`TlsAcceptor::accept()` or `TlsConnector::connect()`.

For the single-session acceptor, the active-session flag is set before the handshake. A peer that
opens TCP but never sends a complete TLS ClientHello holds `active=true` indefinitely, causing
every legitimate connection for that session to be refused. The multi-session acceptor instead
accumulates one permanently stalled spawned task per connection. A stalled TLS server can likewise
hang direct engine startup or a reconnecting initiator on one endpoint indefinitely.

**Impact**: Remote connection-hold/resource-exhaustion DoS and hung startup/reconnect loops.

**Fix direction**: Apply a dedicated TLS-handshake timeout, or include the handshake in
`connect_timeout`/pre-logon timeout. Set the single-session active flag only after a successful
handshake, or ensure timeout/cancellation always clears it.

---

### NEW-152: FileStore appends new records behind a detected corrupt tail, making them disappear on restart

**Category:** Bug

**Severity:** P1

**TF**: `crates/truefix-store/src/file.rs:97-106, 113-136, 611-647`.

`load_index()` stops at an incomplete/corrupt trailing record and returns the valid prefix plus
`corrupted=true`, but it neither truncates the file to the last valid offset nor records that
offset for future appends. `BodyLog::append()` subsequently obtains its offset from the physical
file length and appends after the corrupt bytes.

The current process indexes those new records and can read them. On the next restart,
`load_index()` reaches the same original corruption, stops again, and never discovers any valid
records appended after it.

**Impact**: With the default behavior when `ForceResendWhenCorruptedStore` is not enabled, every
newly stored message after recovery is silently lost from the next process's index. Repeated
restarts can strand an arbitrarily large valid suffix.

**Distinction from NEW-117**: NEW-117 covers advancing the sequence file before a body append
fails. This finding covers successful future appends becoming permanently unreachable because an
already-detected corrupt tail remains physically in front of them.

**Fix direction**: During open, truncate the body file to the last valid record boundary before
allowing further writes (and sync when configured), or make a corrupted store read-only until it
is explicitly reset/repaired.

---

### NEW-153: Biased admin priority can indefinitely starve ticks, controls, and application messages

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-transport/src/lib.rs:912-977`.

The connection-processing loop uses `tokio::select! { biased; ... }` with `admin_rx.recv()` first,
followed by the application queue, ticker, and control channel. Under a sustained admin-message
flood the first branch remains continuously ready, so Tokio always selects it.

**Impact**: Application messages can remain unprocessed indefinitely; heartbeat/schedule/logout
timeouts do not tick; and operator `Logout`, `Reset`, or `Send` controls can become unresponsive.
Together with NEW-150, the starved application path also accelerates unbounded staging growth.

**Fix direction**: Use fair selection, or process at most a bounded admin burst before explicitly
polling application/ticker/control work. Administrative priority should prevent starvation *of*
admin traffic, not permit admin traffic to starve every other lifecycle mechanism.

---

### NEW-154: FileStore mutates in-memory sequence state before persistence succeeds

**Category:** Bug

**Severity:** P2

**TF**: `crates/truefix-store/src/file.rs:241-260`.

`SeqFile::set_sender()` and `set_target()` update the mutex-protected state before calling
`write_seq_value()`:

```rust
self.lock()?.0 = seq;
write_seq_value(&self.sender_path, seq, self.sync)
```

If opening/writing/fsyncing/renaming the temporary file fails, the method returns `Err` but
`next_sender_seq()`/`next_target_seq()` report the unpersisted value for the remainder of the
process. `reset()` similarly changes memory to `(1,1)` before either replacement file is known to
have been created successfully.

**Impact**: Runtime and durable state diverge after a reported I/O error. `RefreshOnLogon` and
other readers can consume the false in-memory value, while the next process restarts from the old
on-disk sequence.

**Fix direction**: Persist first and commit the in-memory state only after success, while holding
the same lock; alternatively restore the prior value on every failure path. Reset needs an
equivalent commit/rollback discipline.

---

### NEW-155: Public `decode()` accepts a CheckSum value that is not exactly three digits

**Category:** Bug

**Severity:** P3

**TF**: `crates/truefix-core/src/codec/decode.rs:212-216`;
`crates/truefix-core/src/framing.rs` assumes the fixed `10=XXX<SOH>` trailer shape.

The decoder validates tag 10 with a generic integer parser:

```rust
let declared_cs = parse_u32(&last.1).ok_or(DecodeError::MissingChecksum)?;
```

It does not require the raw value to be exactly three ASCII digits. A directly supplied message
whose computed checksum is 7 can therefore be accepted with `10=7<SOH>` instead of the canonical
and required `10=007<SOH>`. The stream framing API assumes a seven-byte checksum trailer, so the
two public decoding entry points disagree.

**Impact**: Low-severity wire-format leniency and API inconsistency; malformed messages can pass
`Message::decode()` even though they cannot be framed normally by the transport.

**Fix direction**: Require `last.1.len() == 3` and all three bytes to be ASCII digits before
parsing.

---

## Summary

| Severity | Original (NEW-97–109) | Parallel review (NEW-110–147) | Focused audit (NEW-148–155) | Total |
|----------|----------------------|-------------------------------|-----------------------------|-------|
| P1 (High) | 5 | 0 | 5 | 10 |
| P2 (Medium) | 5 | 16 | 2 | 23 |
| P3 (Low) | 2 | 17 | 1 | 20 |
| **Total** | **12** | **33** | **8** | **53** |

### Suggested fix priority

1. **NEW-148** (dynamic sessions share one store) — cross-session sequence/history corruption;
   requires a per-dynamic-identity store factory or an explicit configuration rejection.
2. **NEW-149** (shutdown leaves accepted sessions running) — Engine shutdown currently does not
   satisfy its resource/lifecycle guarantee.
3. **NEW-150** (`InChanCapacity` bypassed by unbounded staging) — remotely triggerable memory
   exhaustion despite an explicitly configured bound.
4. **NEW-151** (TLS handshake has no timeout) — connection-hold DoS and hung startup/reconnect.
5. **NEW-152** (FileStore appends behind corrupt tail) — successfully written post-recovery
   messages disappear from the next process's index.
6. **NEW-100** (stale teardown Logout in store) — cheapest to fix (`send_raw` instead of
   `send_stored`), prevents a real cross-connection spurious-Logout scenario.
7. **NEW-97** (`on_start_logout` silently drops) — small fix, affects initiator shutdown.
8. **NEW-99** (`SequenceReset` bypasses `validate_app`) — move routing after validation.
9. **NEW-98** (out-of-order Logon seq not advanced on dict-reject) — one-line condition fix.
10. **NEW-101** (`as_int`/`as_decimal` `.trim()`) — remove `.trim()`, narrow blast radius.
11. **NEW-119** (non-gap-fill SequenceReset leaks queue entries) — add `queue.retain` call.
12. **NEW-120** (`drain_queue` missing Logout on error disconnect) — mirror B5/FR-008 fix.
13. **NEW-153** (biased admin priority starvation) — add fairness/bounded admin bursts.
14. **NEW-154** (FileStore mutates memory before persistence) — commit state only after a
    successful durable write.
15. **NEW-115** (accept loop silent exit) — add error logging and continue.
16. **NEW-116** (non-transactional reset for Mongo/SQL/Mssql) — wrap in transaction.
17. **NEW-117** (FileStore seq-then-body crash window) — swap write order.
18. **NEW-118** (NoopStore doesn't track seq nums) — add in-memory tracking.
19. **NEW-104** (`ValidateFieldsOutOfOrder` default `false` → `true`) — one-line default change.
20. **NEW-111** (`AllowPosDup` default `true` → `false`) — one-line default change.
21. **NEW-112** (`EnabledProtocols` silent fallback) — reject unrecognized values.
22. **NEW-113** (`bool_key` leniency) — make strict by default.
23. **NEW-102, NEW-103** (multiplier defaults) — two one-line default changes.
24. **NEW-107** (SOH verification in `frame_length`) — one-line guard.
25. **NEW-121–NEW-123** (Engine/session handle API gaps) — larger scope, schedule for next release.
26. **NEW-124–NEW-126** (log backend gaps) — medium scope, schedule alongside log refactoring.
27. **NEW-127** (header/trailer required-field check) — structural, schedule for next release.
28. **NEW-105, NEW-106** (`CancelOrdersOnDisconnect`, `PossResend`) — feature additions, larger scope.
29. **NEW-108** (file rotation) — documentation only, no code change needed.
30. All remaining P3 items — track normally, schedule as time permits.
